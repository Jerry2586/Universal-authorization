# Web 管理后台《UI功能与接口识别报告》

> 项目：Universal Authorization / 通用 Key 授权服务器
> 项目版本：0.8.0
> 报告编号：UIR-001
> 当前人格：2号人格（资深 UI/UX 设计工程师）
> 扫描日期：2026-08-24
> 扫描方式：只读扫描现有 TypeScript 路由、服务、权限模型、数据库迁移与部署配置
> 当前状态：接口识别完成，尚未编写 UI 代码

---

## 1. 识别结论

现有项目是一个完整度较高的 **API 型授权服务器**，后端已经具备产品、版本、功能、授权策略、Key、设备、审计、授权事件和客户端授权协议等能力，但 **没有前端工程、登录页面、管理后台页面、前端路由、UI 组件库或前端 API 请求层**。

共识别到 **36 个 HTTP 接口**：

| 类别 | 数量 | 说明 |
|---|---:|---|
| 系统状态接口 | 2 | 健康检查、PostgreSQL/Redis 就绪检查 |
| 客户端授权协议接口 | 7 | 挑战、激活、验证、刷新、心跳、释放、自助解绑 |
| 管理员接口 | 27 | 产品、版本、功能、策略、Key、设备、审计、事件 |
| 合计 | **36** | 当前项目全部已注册 HTTP 路由 |

核心结论：

1. 27 个管理员业务接口可以直接映射为管理后台页面和操作。
2. 7 个客户端协议接口不适合做成普通管理员按钮，应放入“接口中心”展示用途和调用要求。
3. “像普通网站一样账号密码登录”目前无法由现有接口完成，必须补充管理员认证会话接口。
4. 当前列表接口大多只返回 `items + limit + offset`，没有 `total`，前端只能实现“上一页/下一页”，不能准确显示总页数。
5. Key 明文只在生成成功时返回一次，UI 必须使用一次性交付页面，禁止假设可以再次查看完整 Key。

---

## 2. 已有功能总览

### 2.1 产品中心

- 创建产品。
- 查询产品列表和产品详情。
- 修改产品名称、说明、状态和 JSON 设置。
- 配置最低版本、推荐版本、强制更新版本。
- 创建、查询和修改产品版本。
- 版本状态：`ACTIVE`、`BLOCKED`、`DEPRECATED`。
- 创建、查询和修改产品功能定义。
- 功能状态：`ACTIVE`、`DISABLED`。

### 2.2 授权策略中心

- 创建、查询和修改授权策略。
- 授权类型：`TRIAL`、`DURATION`、`FIXED_EXPIRY`、`PERPETUAL`。
- 配置有效时长、最大设备数、最大并发会话数、离线宽限期、自助解绑和解绑冷却时间。
- 使用 JSON `rules` 承载固定到期时间及扩展规则。

### 2.3 Key 授权中心

- 单个生成 Key。
- 批量生成 1～500 个 Key。
- 按产品、状态分页查询 Key。
- 查询 Key 详情。
- 暂停、恢复、续期和永久吊销 Key。
- 配置功能授权、功能限制、元数据、设备数和并发数。
- Key 状态：`CREATED`、`ACTIVE`、`SUSPENDED`、`EXPIRED`、`REVOKED`、`DISABLED`。

### 2.4 设备中心

- 在 Key 详情下查询绑定设备。
- 按激活状态过滤。
- 管理员强制解绑。
- 封禁设备。
- 解封设备。
- 查看设备平台、系统版本、风险分、最后在线时间、会话数和封禁原因。

### 2.5 审计中心

- 查询管理员审计日志。
- 查询授权事件。
- 支持按主体、动作、资源、结果、请求 ID、产品、Key、设备、会话和时间范围筛选。

### 2.6 客户端授权协议

- 获取服务器挑战值。
- 使用 Key 激活设备。
- 在线验证授权。
- 刷新授权令牌。
- 会话心跳。
- 主动释放会话。
- 设备自助解绑。

### 2.7 运维状态

- 服务健康检查。
- PostgreSQL 和 Redis 就绪状态及延迟检查。

---

## 3. 用户角色与权限

### 3.1 已识别主体类型

| 角色类型 | tenant_id | 能力 |
|---|---|---|
| 平台管理员 | `null` | 可跨租户，但请求必须通过 `X-Tenant-Id` 指定目标租户 |
| 租户管理员 | 固定租户 UUID | 只能访问自己租户且必须具备对应权限 |
| 授权客户端设备 | 不适用 | 使用设备证书、授权令牌、Ed25519 签名调用客户端协议 |

### 3.2 已定义权限码

| 权限码 | 当前是否有对应 API | 后台用途 |
|---|---|---|
| `products.read` | 有 | 查看产品、版本和功能 |
| `products.write` | 有 | 创建和修改产品、版本和功能 |
| `licenses.read` | 有 | 查看策略和 Key |
| `licenses.write` | 有 | 创建/修改策略，生成和操作 Key |
| `licenses.export` | 有 | 批量生成并交付 Key |
| `devices.read` | 有 | 查看 Key 绑定设备 |
| `devices.unbind` | 有 | 强制解绑设备 |
| `devices.block` | 有 | 封禁和解封设备 |
| `audit.read` | 有 | 查看审计日志和授权事件 |
| `platform.tenants.manage` | 无管理 API | 暂不能制作租户管理按钮 |
| `admin.users.manage` | 无管理 API | 暂不能制作管理员管理按钮 |
| `admin.roles.manage` | 无管理 API | 暂不能制作角色管理按钮 |
| `settings.manage` | 无管理 API | 暂不能制作系统设置按钮 |
| `signing-keys.manage` | 无管理 API | 暂不能制作签名密钥管理按钮 |

### 3.3 当前管理员请求认证

所有管理员接口依赖以下请求头：

```http
Authorization: Bearer <MANAGEMENT_GATEWAY_TOKEN>
X-Admin-User-Id: <管理员 UUID>
X-Tenant-Id: <平台管理员目标租户 UUID，可选/按主体决定>
```

风险：浏览器前端不能把 `MANAGEMENT_GATEWAY_TOKEN` 写入静态 JavaScript、LocalStorage 或页面配置，否则任何访问者都可能取得网关密钥。

---

## 4. 当前页面现状

### 4.1 用户前台页面

当前没有用户前台页面。授权客户端通过 SDK 或程序代码调用 `/api/v1/*` 协议接口。

### 4.2 管理后台页面

当前没有管理后台页面，也没有：

- 前端框架和构建配置。
- 登录页。
- 页面路由。
- 前端 API 请求封装。
- 表格、表单、弹窗、通知等 UI 组件。
- 颜色、字体、间距或响应式规范。

---

## 5. 运行端口与部署入口

| 项目 | 当前值 |
|---|---|
| Fastify 默认监听地址 | `127.0.0.1`，部署配置可覆盖为 `0.0.0.0` |
| Fastify 默认端口 | `3000` |
| 健康检查 | `/health` |
| 就绪检查 | `/ready` |
| 管理员 API 前缀 | `/admin/v1` |
| 客户端 API 前缀 | `/api/v1` |
| Web 管理后台 | 当前不存在 |

建议后续采用同域方式：用户访问 `http(s)://服务器/admin/`，页面通过同域 Cookie 调用管理接口，避免把内部网关 Token 暴露到浏览器。

---

## 6. API 公共返回结构

成功响应：

```json
{
  "request_id": "请求追踪 ID",
  "success": true,
  "code": "OK",
  "message": "中文操作结果",
  "server_time": "ISO-8601 时间",
  "data": {}
}
```

失败响应：

```json
{
  "request_id": "请求追踪 ID",
  "success": false,
  "code": "错误码",
  "message": "中文错误信息",
  "server_time": "ISO-8601 时间",
  "data": {
    "retryable": false,
    "details": {}
  }
}
```

UI 必须统一处理：加载中、成功、空数据、参数错误、未登录、无权限、资源不存在、服务不可用、可重试错误和未知错误。

---

## 7. 管理后台页面地图

```text
PAGE-001 登录页（依赖新认证接口）
PAGE-002 后台首页
├─ 服务健康状态
├─ PostgreSQL / Redis 状态
├─ 产品与 Key 概览（统计接口缺失时使用有限降级）
└─ 最近授权事件

PAGE-003 产品列表
├─ 新建产品
├─ 产品状态
└─ 进入产品详情

PAGE-004 产品详情
├─ 基本资料
├─ 客户端版本规则
├─ 产品版本
└─ 功能定义

PAGE-005 授权策略
├─ 策略列表
├─ 新建策略
└─ 修改策略

PAGE-006 Key 列表
├─ 产品/状态筛选
├─ 单个生成
├─ 批量生成
└─ 进入 Key 详情

PAGE-007 Key 一次性交付
├─ 明文 Key 展示
├─ 一键复制
├─ 批量下载
└─ “我已安全保存”确认

PAGE-008 Key 详情
├─ 授权信息
├─ 功能授权和限制
├─ 暂停/恢复/续期/吊销
└─ 绑定设备

PAGE-009 管理员审计日志
PAGE-010 授权事件
PAGE-011 接口中心
├─ 系统状态接口
├─ 管理员 API 清单
└─ 客户端授权协议说明

PAGE-012 通用状态页
├─ 403 无权限
├─ 404 页面不存在
├─ 500 系统异常
└─ 服务维护/基础设施未就绪
```

---

## 8. 36 个接口与页面对应表

### 8.1 系统状态接口（2 个）

| 编号 | 页面 | 功能 | 接口 | 方法 | 输入 | 主要返回字段 | 页面状态 | 权限 |
|---|---|---|---|---|---|---|---|---|
| API-001 | PAGE-002/PAGE-011 | 服务存活检查 | `/health` | GET | 无 | `status, service, version` | 健康/异常/加载 | 无 |
| API-002 | PAGE-002/PAGE-011 | 基础设施就绪检查 | `/ready` | GET | 无 | `status, components`，组件含状态和延迟 | 就绪/503未就绪/加载 | 无 |

### 8.2 客户端授权协议接口（7 个）

> 这些接口只在 PAGE-011 展示说明，不直接制作“代替客户端执行”的普通按钮。

| 编号 | 页面 | 功能 | 接口 | 方法 | 输入摘要 | 主要返回字段 | 页面状态 | 权限/安全 |
|---|---|---|---|---|---|---|---|---|
| API-003 | PAGE-011 | 签发挑战值 | `/api/v1/challenges` | POST | `product_code, client_nonce` | `server_nonce, issued_at, expires_at, protocol_version` | 文档展示 | 无管理员权限 |
| API-004 | PAGE-011 | 激活 Key 与设备 | `/api/v1/licenses/activate` | POST | Key、设备公钥、指纹、平台、版本、挑战值、随机数、时间戳、幂等键及签名头 | 激活结果、设备证书、授权令牌、会话信息 | 文档展示 | Ed25519 设备签名 |
| API-005 | PAGE-011 | 在线验证 | `/api/v1/licenses/verify` | POST | `device_certificate, license_token, client_version, timestamp, client_nonce` 及设备签名头 | 验证结果、授权状态、刷新提示等 | 文档展示 | 证书+令牌+设备签名 |
| API-006 | PAGE-011 | 刷新授权令牌 | `/api/v1/licenses/refresh` | POST | 验证字段 + `idempotency_key` | 新授权令牌及签发/到期信息 | 文档展示 | 证书+令牌+签名+幂等 |
| API-007 | PAGE-011 | 会话心跳 | `/api/v1/sessions/heartbeat` | POST | 证书、令牌、会话、序号、时间和签名信息 | 在线状态、下次心跳等 | 文档展示 | 设备认证协议 |
| API-008 | PAGE-011 | 主动释放会话 | `/api/v1/sessions/release` | POST | 证书、令牌、会话、幂等键和签名信息 | 释放结果和时间 | 文档展示 | 设备认证协议 |
| API-009 | PAGE-011 | 设备自助解绑 | `/api/v1/devices/unbind` | POST | 证书、令牌、幂等键、原因和签名信息 | 解绑结果和时间 | 文档展示 | 策略允许+设备认证 |

### 8.3 产品、版本和功能接口（10 个）

| 编号 | 页面 | 功能 | 接口 | 方法 | 输入参数 | 主要返回字段 | 页面状态 | 权限 |
|---|---|---|---|---|---|---|---|---|
| API-010 | PAGE-003 | 创建产品 | `/admin/v1/products` | POST | `code, name, description?, status, minimum_client_version?, recommended_client_version?, force_update_version?, settings` | 产品完整对象 | 表单/提交/成功/错误 | `products.write` |
| API-011 | PAGE-003 | 产品列表 | `/admin/v1/products` | GET | `limit, offset` | `items, limit, offset` | 加载/空/列表/错误 | `products.read` |
| API-012 | PAGE-004 | 产品详情 | `/admin/v1/products/:productId` | GET | 路径 `productId` | `id, tenant_id, code, name, description, status, version规则, settings, created_at, updated_at` | 加载/详情/404/错误 | `products.read` |
| API-013 | PAGE-004 | 修改产品 | `/admin/v1/products/:productId` | PATCH | 除 `code` 外至少一个可修改字段 | 修改后的产品对象 | 编辑/提交/成功/错误 | `products.write` |
| API-014 | PAGE-004 | 创建产品版本 | `/admin/v1/products/:productId/versions` | POST | `version, status, force_update, release_notes?, released_at?` | 版本对象 | 弹窗/提交/成功/错误 | `products.write` |
| API-015 | PAGE-004 | 产品版本列表 | `/admin/v1/products/:productId/versions` | GET | `productId` | `items[]`：版本、状态、强制更新、说明、发布时间 | 加载/空/列表/错误 | `products.read` |
| API-016 | PAGE-004 | 修改产品版本 | `/admin/v1/products/:productId/versions/:versionId` | PATCH | 至少一个版本可修改字段 | 修改后的版本对象 | 编辑/提交/成功/错误 | `products.write` |
| API-017 | PAGE-004 | 创建功能定义 | `/admin/v1/products/:productId/features` | POST | `code, name, description?, status` | 功能对象 | 弹窗/提交/成功/错误 | `products.write` |
| API-018 | PAGE-004 | 功能列表 | `/admin/v1/products/:productId/features` | GET | `productId` | `items[]`：编码、名称、说明、状态、时间 | 加载/空/列表/错误 | `products.read` |
| API-019 | PAGE-004 | 修改功能定义 | `/admin/v1/products/:productId/features/:featureId` | PATCH | `name?, description?, status?`，至少一项 | 修改后的功能对象 | 编辑/提交/成功/错误 | `products.write` |

### 8.4 授权策略接口（3 个）

| 编号 | 页面 | 功能 | 接口 | 方法 | 输入参数 | 主要返回字段 | 页面状态 | 权限 |
|---|---|---|---|---|---|---|---|---|
| API-020 | PAGE-005 | 创建授权策略 | `/admin/v1/license-policies` | POST | `product_id?, code, name, license_type, duration_seconds?, max_devices, max_concurrent_sessions, offline_grace_seconds, allow_self_unbind, unbind_cooldown_seconds, rules, status` | 策略完整对象 | 表单/联动校验/成功/错误 | `licenses.write` |
| API-021 | PAGE-005 | 策略列表 | `/admin/v1/license-policies` | GET | `product_id?, limit, offset` | `items, limit, offset` | 加载/空/列表/错误 | `licenses.read` |
| API-022 | PAGE-005 | 修改策略 | `/admin/v1/license-policies/:policyId` | PATCH | 至少一个策略字段 | 修改后的策略对象 | 编辑/提交/成功/错误 | `licenses.write` |

策略表单必须按授权类型动态约束：

- `TRIAL` / `DURATION`：必须设置 `duration_seconds`。
- `FIXED_EXPIRY`：必须在 `rules.fixed_expires_at` 设置未来时间。
- `PERPETUAL`：不允许配置时长或固定到期时间。

### 8.5 Key 接口（9 个）

| 编号 | 页面 | 功能 | 接口 | 方法 | 输入参数 | 主要返回字段 | 页面状态 | 权限 |
|---|---|---|---|---|---|---|---|---|
| API-023 | PAGE-006/PAGE-007 | 单个生成 Key | `/admin/v1/license-keys` | POST | `product_id, policy_id, max_devices?, max_concurrent_sessions?, metadata, features[]` | `generation_batch_id, plain_key, license` | 生成中/一次性交付/错误 | `licenses.write` |
| API-024 | PAGE-006/PAGE-007 | 批量生成 Key | `/admin/v1/license-keys/batch` | POST | 单个生成字段 + `count(1..500)` | `generation_batch_id, count, items[{plain_key, license}]` | 生成中/一次性交付/下载/错误 | `licenses.write` + `licenses.export` |
| API-025 | PAGE-006 | Key 列表 | `/admin/v1/license-keys` | GET | `product_id?, status?, limit, offset` | `items, limit, offset` | 加载/空/列表/错误 | `licenses.read` |
| API-026 | PAGE-008 | Key 详情 | `/admin/v1/license-keys/:licenseId` | GET | `licenseId` | 脱敏 Key、状态、类型、有效期、设备/并发限制、元数据、功能授权和时间字段 | 加载/详情/404/错误 | `licenses.read` |
| API-027 | PAGE-008 | 暂停 Key | `/admin/v1/license-keys/:licenseId/suspend` | POST | `licenseId` | 修改后的 Key | 确认/处理中/成功/错误 | `licenses.write` |
| API-028 | PAGE-008 | 恢复 Key | `/admin/v1/license-keys/:licenseId/resume` | POST | `licenseId` | 修改后的 Key | 确认/处理中/成功/错误 | `licenses.write` |
| API-029 | PAGE-008 | 续期 Key | `/admin/v1/license-keys/:licenseId/renew` | POST | `extend_seconds` 或 `expires_at`，二选一 | 修改后的 Key | 表单/处理中/成功/错误 | `licenses.write` |
| API-030 | PAGE-008 | 永久吊销 Key | `/admin/v1/license-keys/:licenseId/revoke` | POST | `reason(1..1000)` | 吊销后的 Key | 强二次确认/处理中/成功/错误 | `licenses.write` |

> Key 完整明文只会在 API-023/API-024 成功响应中出现一次；API-025/API-026 只返回 `display_key` 脱敏展示值。

### 8.6 设备接口（4 个）

| 编号 | 页面 | 功能 | 接口 | 方法 | 输入参数 | 主要返回字段 | 页面状态 | 权限 |
|---|---|---|---|---|---|---|---|---|
| API-031 | PAGE-008 | 查看 Key 绑定设备 | `/admin/v1/license-keys/:licenseId/devices` | GET | `activation_status?, limit, offset` | `items, limit, offset`；设备、激活、平台、风险、会话和封禁字段 | 加载/空/列表/错误 | `devices.read` |
| API-032 | PAGE-008 | 强制解绑设备 | `/admin/v1/devices/:deviceId/unbind` | POST | `license_id, reason(1..255)` | `changed` 等操作结果 | 原因输入/确认/成功/错误 | `devices.unbind` |
| API-033 | PAGE-008 | 封禁设备 | `/admin/v1/devices/:deviceId/block` | POST | `reason(1..255)` | `changed` 等操作结果 | 原因输入/确认/成功/错误 | `devices.block` |
| API-034 | PAGE-008 | 解封设备 | `/admin/v1/devices/:deviceId/unblock` | POST | `reason(1..255)` | `changed` 等操作结果 | 原因输入/确认/成功/错误 | `devices.block` |

关键业务提示：强制解绑或封禁会撤销有效会话；解封不会恢复旧绑定和旧会话。

### 8.7 审计与授权事件接口（2 个）

| 编号 | 页面 | 功能 | 接口 | 方法 | 输入参数 | 主要返回字段 | 页面状态 | 权限 |
|---|---|---|---|---|---|---|---|---|
| API-035 | PAGE-009 | 管理员审计日志 | `/admin/v1/audit-logs` | GET | `actor_type, actor_id, action, resource_type, resource_id, result, request_id, occurred_from, occurred_to, limit, offset` 均可选 | 主体、动作、资源、请求、IP、UA、结果、前后数据、元数据、时间 | 加载/空/筛选/详情抽屉/错误 | `audit.read` |
| API-036 | PAGE-010 | 授权事件 | `/admin/v1/license-events` | GET | `product_id, license_id, device_id, activation_id, session_id, event_type, result, reason_code, request_id, occurred_from, occurred_to, limit, offset` 均可选 | 产品、Key、设备、激活、会话、事件、结果、原因、IP、元数据、时间 | 加载/空/筛选/详情抽屉/错误 | `audit.read` |

---

## 9. 页面需要显示的数据与允许操作

| 页面 | 必须显示 | 允许操作 | 禁止凭空增加的操作 |
|---|---|---|---|
| 登录 | 邮箱、密码、登录状态 | 登录、退出（依赖补充接口） | 注册、找回密码、MFA，除非后端补充 |
| 后台首页 | 服务版本、健康状态、PG/Redis 状态、近期事件 | 刷新 | 无统计接口时禁止伪造数字 |
| 产品列表 | 编码、名称、状态、版本规则、更新时间 | 创建、查看、编辑 | 删除、复制产品 |
| 产品详情 | 基础资料、版本、功能定义 | 编辑、创建/修改版本和功能 | 删除版本、删除功能 |
| 授权策略 | 类型、时长、设备数、并发数、解绑规则、状态 | 创建、编辑 | 删除、复制策略 |
| Key 列表 | 脱敏 Key、产品、状态、类型、到期时间、限制 | 筛选、生成、查看 | 查看历史完整 Key、删除 Key |
| Key 交付 | 本次生成的完整 Key、批次号 | 复制、下载、确认已保存 | 页面离开后再次取明文 |
| Key 详情 | 授权信息、功能、元数据、设备 | 暂停、恢复、续期、吊销、设备操作 | 修改 Key 字符串、恢复已吊销 Key |
| 审计日志 | 动作、主体、资源、结果、IP、时间、前后数据 | 筛选、查看详情 | 修改或删除审计记录 |
| 授权事件 | 事件类型、关联实体、结果、原因、时间 | 筛选、查看详情 | 修改或删除事件 |
| 接口中心 | 36 个接口、用途、认证方式 | 复制接口路径 | 绕过签名直接模拟安全协议 |

---

## 10. 缺失接口与字段不足

### 10.1 阻断普通网站登录的缺失接口

1. 管理员账号密码登录。
2. 管理员退出。
3. 获取当前管理员资料和权限。
4. 刷新/延长会话或明确会话过期机制。
5. 首次部署创建首个租户和管理员。

### 10.2 影响后台体验的缺失接口

1. 仪表盘统计。
2. 所有列表的 `total` 总数。
3. 产品搜索与状态筛选。
4. Key 按脱敏码、批次、到期范围搜索。
5. 全局设备列表。
6. 当前在线会话列表。
7. 租户选择和租户管理。
8. 管理员、角色、权限管理。
9. 系统设置和签名密钥管理。
10. 导出审计日志/授权事件接口。

### 10.3 数据库已有但没有管理 API 的数据域

- `tenants`
- `admin_users`
- `roles`
- `permissions`
- `admin_user_roles`
- `role_permissions`
- `api_clients`
- `license_sessions`
- `session_heartbeats`
- `signing_keys`
- `system_settings`

这些数据域不能直接制作可操作页面按钮，必须先补充对应后端接口。

---

## 11. UI 设计风险

| 风险编号 | 风险 | 影响 | UI 对策 |
|---|---|---|---|
| RISK-UI-001 | 网关 Token 暴露给浏览器 | 严重安全事故 | 必须改为服务端会话/HttpOnly Cookie |
| RISK-UI-002 | 没有登录 API | 无法实现普通网站式登录 | 先完成认证技术依赖 |
| RISK-UI-003 | 列表没有 `total` | 无法准确显示页数 | 首版使用上一页/下一页，后端后续补 total |
| RISK-UI-004 | Key 明文只返回一次 | 用户离开页面后无法找回 | 强提醒、复制、下载和离开拦截 |
| RISK-UI-005 | 永久吊销不可恢复 | 误操作造成损失 | 输入原因、二次确认、突出不可恢复 |
| RISK-UI-006 | JSON settings/rules/metadata | 普通用户难理解 | 基础表单 + 高级 JSON 编辑区 + 校验 |
| RISK-UI-007 | 平台管理员必须选租户 | 未指定会返回 400 | 顶部租户上下文选择器；接口缺失前不能自动加载租户 |
| RISK-UI-008 | 权限粒度较细 | 页面可能显示无权按钮 | 根据“当前管理员权限接口”隐藏/禁用操作 |
| RISK-UI-009 | 客户端协议需要签名 | Web 调试器容易误导 | 首版只做说明，不提供绕过安全的调用按钮 |
| RISK-UI-010 | 无静态资源托管 | 部署后仍只有 API | 需增加 `/admin/` 静态资源与 SPA 回退方案 |

---

## 12. 建议的管理后台功能模块

| 模块编号 | 模块名称 | 涉及页面 | 接口 | 当前状态 |
|---|---|---|---|---|
| MOD-UI-001 | 管理员认证与后台壳 | PAGE-001、PAGE-012 | 依赖新增认证接口 | 技术依赖待评估 |
| MOD-UI-002 | 仪表盘与服务状态 | PAGE-002 | API-001、API-002、API-036 | 可设计，统计能力部分缺失 |
| MOD-UI-003 | 产品、版本与功能管理 | PAGE-003、PAGE-004 | API-010～API-019 | 后端已支持 |
| MOD-UI-004 | 授权策略管理 | PAGE-005 | API-020～API-022 | 后端已支持 |
| MOD-UI-005 | Key 生成、列表与交付 | PAGE-006、PAGE-007 | API-023～API-025 | 后端已支持 |
| MOD-UI-006 | Key 详情与生命周期 | PAGE-008 | API-026～API-030 | 后端已支持 |
| MOD-UI-007 | 设备管理 | PAGE-008 | API-031～API-034 | 后端已支持 |
| MOD-UI-008 | 审计与授权事件 | PAGE-009、PAGE-010 | API-035～API-036 | 后端已支持 |
| MOD-UI-009 | API 接口中心 | PAGE-011 | API-001～API-036 | 可设计为只读说明 |
| MOD-UI-010 | Linux 同域部署整合 | `/admin/` | 静态托管/反向代理依赖 | 技术依赖待评估 |

---

## 13. 本阶段验收结论

- [x] 已识别全部 36 个 HTTP 接口。
- [x] 已区分运行端口和 API 端点。
- [x] 已识别管理员认证、租户和权限规则。
- [x] 已识别页面可显示字段、可执行操作和禁止虚构操作。
- [x] 已识别 Key 一次性交付等关键安全限制。
- [x] 已识别没有前端和登录闭环的事实。
- [x] 已形成页面地图和模块地图。
- [x] 已选择视觉方向：后台 UI-01 为主，Key 管理页面采用后台 UI-03。
- [x] 已生成 3 套管理后台效果图。
- [ ] 已生成《UI设计基线》，等待用户确认后冻结。
- [ ] 按 V5.0 门禁，尚未进入正式 UI 编码阶段。
