# 16-管理后台成熟化第一阶段设计与使用

## 1. 本阶段目标

本阶段不是美化演示页面，而是补齐成熟后台最基础的真实管理能力：

- 管理员账号列表、搜索、状态筛选、真实总数分页、新增、编辑、暂停和禁用。
- 管理员密码重置，重置后立即使旧会话失效。
- 个人中心修改显示名称。
- 管理员修改自己的密码，修改后强制重新登录。
- 角色列表、自定义角色创建、权限矩阵和权限更新。
- 工作区基础设置持久化到 PostgreSQL `system_settings`。
- 所有敏感操作写入 `audit_logs`。

## 2. 页面入口

| 页面 | 地址 | 所需权限 |
|---|---|---|
| 管理员与角色 | `/admin/admin-users` | `admin.users.manage` |
| 个人中心 | `/admin/profile` | 已登录管理员 |
| 系统设置 | `/admin/settings` | `tenant.settings.manage` |

## 3. 新增真实 API

- `GET /admin/v1/admin-users`
- `POST /admin/v1/admin-users`
- `PATCH /admin/v1/admin-users/:adminId`
- `POST /admin/v1/admin-users/:adminId/reset-password`
- `PATCH /admin/v1/profile`
- `POST /admin/v1/profile/change-password`
- `GET /admin/v1/admin-roles`
- `GET /admin/v1/admin-permissions`
- `POST /admin/v1/admin-roles`
- `PATCH /admin/v1/admin-roles/:roleId`
- `GET /admin/v1/settings/tenant`
- `PUT /admin/v1/settings/tenant`

## 4. 安全规则

1. 密码至少 12 位，必须包含大写字母、小写字母和数字。
2. 密码只保存 scrypt 哈希，不保存明文。
3. 管理员不能停用自己，也不能修改自己的角色，防止误锁后台。
4. 工作区必须保留至少一名启用状态的 `owner`；事务级并发锁会阻止两个并发操作同时移除最后的所有者。
5. 系统 `owner` 角色不可修改。
6. 租户角色不能获得平台专属权限；迁移会清理历史引导脚本可能产生的错误授权。
7. 创建管理员时不默认选择 `owner`，必须明确选择至少一个角色，遵循最小权限原则。
8. 管理员停用、禁用、密码重置或修改密码时增加 `session_version`，所有旧 Cookie 会话立即失效。
9. 安装脚本显式重置引导管理员密码时同样增加会话版本，并记录密码变更时间。
10. 新增、修改、重置密码、角色和设置操作全部写入审计日志。
11. 只有角色管理权限的账号也可以读取角色列表并进入角色页，不会被管理员账号权限错误拦截。
12. 平台管理员修改自己的资料和密码时不需要伪造或额外指定目标租户，个人中心只操作当前登录账号。
13. 管理员引导脚本只会给租户 owner 授予租户可用权限，不会在迁移清理后重新写入平台专属权限。
14. 管理员角色和角色权限不接受重复值，HTTP 层返回 400，Repository 同时防御性去重，避免数据库主键冲突变成 500。
15. 角色或权限目录读取失败时，前端会停用依赖这些数据的新增和编辑入口，不提供无法完成的演示操作。

## 5. 数据库迁移

新增迁移：`database/migrations/0008_admin_console_foundation.sql`。

部署更新时一键脚本会正常执行 `pnpm db:migrate`，现有 `owner` 角色会自动获得新增的 `tenant.settings.manage` 权限，不需要删除数据库或重新安装。迁移还会清理租户角色中的平台专属权限，修复早期引导脚本可能留下的越权授权。

## 6. 傻瓜式使用

1. 重新执行原 Linux 一键安装命令更新程序。
2. 使用安装完成时显示的管理员账号登录 `/admin/`。
3. 左侧进入“管理员与角色”创建管理员和角色。
4. 点击右上角账号菜单进入“个人中心”修改名称或密码。
5. 左侧进入“系统设置”保存工作区默认参数。

> 修改自己的密码后跳回登录页属于正常安全行为，请使用新密码重新登录。

## 7. 本阶段验证

- 后端 TypeScript 类型检查。
- 前端 TypeScript 类型检查。
- 管理员自锁保护、自己密码修改、他人密码重置、CSRF、状态筛选、重复角色/权限拒绝和角色权限入口自动化测试。
- 服务端全部测试与 Web 接口目录契约测试。
- Web 管理后台测试与生产构建。

> 真实 PostgreSQL 迁移会由 Linux 一键部署容器在启动时执行；本机没有 Docker 时只能完成静态迁移契约验证，部署前不会伪称已经在本机容器执行。
