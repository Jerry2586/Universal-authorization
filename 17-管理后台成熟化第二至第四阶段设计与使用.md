# 管理后台成熟化第二至第四阶段设计与使用

本文对应 Web 管理后台成熟化整改的第二、第三、第四阶段。目标不是制作演示页面，而是让页面错误、分页、部署和登录信息都以真实服务端结果为准。

## 1. 最终效果

完成本阶段后：

- 产品、策略、Key、设备、审计日志和授权事件全部读取真实 API。
- PostgreSQL 查询返回真实总条数，前端不再猜测总数。
- API 读取失败时显示真实错误和“重新加载”按钮，不再伪装成“暂无数据”。
- 依赖数据读取失败时，相关新增或编辑操作会被禁止，避免提交错误关联数据。
- 删除、筛选或切换对象后分页越界，页面会自动回到最后一个有效页。
- Linux 一键安装必须同时验收健康接口、就绪接口和 Web 管理后台 HTML。
- 安装成功区直接显示后台地址、管理员账号、工作区代码和随机初始密码。
- 所有数据都来自 PostgreSQL、Redis 和真实管理 API；没有 Mock 数据、演示数据或假按钮。

## 2. 第二阶段：真实错误状态

### 2.1 解决的问题

旧页面可能把网络错误、权限错误或服务器错误显示成“暂无数据”。这会让使用者误以为数据库中真的没有记录。

第二阶段统一增加了查询错误组件。请求失败后页面会显示：

1. 错误标题。
2. 服务端返回的真实错误信息。
3. “重新加载”按钮。
4. 点击按钮后执行当前查询的真实 refetch，而不是刷新假数据。

### 2.2 已覆盖页面

- 产品管理：产品、版本、功能定义和详情。
- 授权策略：产品依赖和策略列表。
- Key 管理：产品、策略、功能、Key 详情和绑定设备。
- 审计中心：审计日志和授权事件。

### 2.3 依赖失败保护

例如“生成 Key”必须先取得真实产品和策略。如果产品接口失败，系统不会继续允许提交一个无法校验的产品 ID。

保护规则：

- 产品选项失败：禁止新建或编辑策略。
- 产品、策略或功能选项失败：禁止生成 Key。
- 详情接口失败：显示错误，不把失败结果画成空详情。
- 重试成功后：自动恢复正常操作。

## 3. 第三阶段：真实 total 与安全分页

### 3.1 真实分页返回格式

以下列表接口统一返回：

```json
{
  "items": [],
  "total": 0,
  "limit": 20,
  "offset": 0
}
```

`total` 是数据库在同一租户、同一筛选条件下执行 `COUNT(*)` 得到的真实总数，不再使用当前页长度推算。

### 3.2 已完成真实 total 的接口

- 产品列表。
- 授权策略列表。
- Key 列表。
- 指定 Key 的绑定设备列表。
- 管理员审计日志列表。
- 授权事件列表。

所有查询都满足：

- 列表 SQL 和计数 SQL 使用相同租户条件。
- 列表 SQL 和计数 SQL 使用相同筛选条件。
- 参数化查询，不拼接用户输入。
- `limit` 和 `offset` 只用于列表，不影响真实总数。

### 3.3 前端分页边界

前端使用服务端 `total` 计算总页数：

- 总数变成 0：自动回到第一页。
- 当前 offset 大于等于总数：自动回到最后一个有效页。
- 筛选条件变化：从第一页重新查询。
- 接口失败：隐藏分页并显示真实错误。
- 全量选项读取：优先按真实 `total` 停止，同时兼容旧版没有 total 的接口。

## 4. 第四阶段：一键安装和真实验收

### 4.1 Linux 一键安装命令

SSH 登录 Linux 服务器，只执行：

```bash
curl -fsSL https://raw.githubusercontent.com/Jerry2586/Universal-authorization/main/install.sh | sudo bash
```

没有 curl 时：

```bash
wget -qO- https://raw.githubusercontent.com/Jerry2586/Universal-authorization/main/install.sh | sudo bash
```

默认安装目录：

```text
/opt/universal-authorization
```

### 4.2 安装器执行顺序

1. 检查 root 权限和安全安装目录。
2. 安装或检查 Git、curl、Docker Engine 和 Docker Compose v2。
3. 检查远端分支真实存在。
4. 拉取或快进更新源码，不覆盖安装目录中的未提交改动。
5. 生成或补全安全的 `.env`。
6. 构建并启动 PostgreSQL、Redis 和授权服务器。
7. 自动执行数据库迁移和管理员引导。
8. 等待 Docker 容器进入 healthy。
9. 在应用容器内真实请求 `/health`，必须返回 HTTP 200。
10. 在应用容器内真实请求 `/ready`，必须返回 HTTP 200。
11. 在应用容器内真实请求 `/admin/`，必须返回 HTTP 200 和 HTML。
12. 三项全部通过后，才输出“安装成功”和后台登录信息。

任一验收失败，脚本会停止并显示容器状态及最近 120 行应用日志，不会伪报安装成功。

### 4.3 安装完成后看到什么

终端会直接显示：

```text
后台登录地址（远程）：http://服务器IP:3000/admin/
管理员账号：admin@example.com
工作区代码：default
管理员初始密码：随机生成的真实密码
```

还会明确显示后台、健康和就绪地址。

登录信息保存到：

```text
/opt/universal-authorization/admin-login.txt
```

文件权限为 `600`。忘记信息时执行：

```bash
sudo cat /opt/universal-authorization/admin-login.txt
```

或：

```bash
cd /opt/universal-authorization
sudo ./show-admin-login.sh
```

### 4.4 重装不会重置密码

`admin-login.txt` 保存的是首次引导时写入 `.env` 的初始密码。

- 管理员第一次创建时，该密码可以直接登录。
- 管理员已经存在时，重新执行安装不会自动重置数据库密码。
- 如果管理员已经在后台修改密码，应使用修改后的密码。
- 数据库只保存密码 Hash，脚本不能反查修改后的明文密码。

这是安全设计，避免每次更新服务都把管理员密码改回旧值。

## 5. 新手登录步骤

1. 浏览器打开 `http://服务器IP:3000/admin/`。
2. “工作区代码”填写 `default`，除非部署时自定义过。
3. “管理员账号”填写安装结果中的邮箱。
4. “密码”填写安装结果中的随机初始密码。
5. 登录成功后进入个人中心修改密码。
6. 妥善保存新密码；修改后 `admin-login.txt` 不会自动记录明文新密码。

## 6. 常用排查命令

```bash
cd /opt/universal-authorization

docker compose ps
docker compose logs --tail 120 app
sudo ./show-admin-login.sh

curl -i http://127.0.0.1:3000/health
curl -i http://127.0.0.1:3000/ready
curl -i http://127.0.0.1:3000/admin/
```

判断方法：

- 三个地址在服务器本机都正常，但外网打不开：检查云安全组和系统防火墙的 TCP 3000 端口。
- `/health` 正常、`/ready` 失败：重点检查 PostgreSQL、Redis 和应用日志。
- `/admin/` 不是 HTML：重新执行一键安装，确认前端构建已进入镜像。
- 登录失败：先确认工作区代码、邮箱和密码没有复制空格，再查看应用日志。

## 7. 代码位置

- 统一查询错误：`web/src/components/Ui.tsx`
- 产品分页：`web/src/pages/ProductsPage.tsx`
- 策略分页：`web/src/pages/PoliciesPage.tsx`
- Key 和设备分页：`web/src/pages/KeysPage.tsx`
- 审计分页：`web/src/pages/AuditPage.tsx`
- 后端分页结果：`src/shared/pagination/page-result.ts`
- Linux 拉取安装：`install.sh`
- Docker 构建和真实验收：`deploy.sh`
- 登录信息输出：`show-admin-login.sh`

## 8. 验收清单

部署发布前必须通过：

```bash
pnpm typecheck
pnpm build:all
pnpm test
pnpm --dir web test
bash -n install.sh
bash -n deploy.sh
bash -n show-admin-login.sh
docker compose config
git diff --check
```

如果当前开发机没有 Docker，只能确认代码、测试、脚本语法和 Compose 配置；不得声称已经完成真实容器运行验收。Linux 安装器运行时会再次执行三项真实 HTTP 验收。
