# Web 管理后台全接口真实对接说明

本文说明当前 Web 管理后台如何连接授权服务器的真实接口。后台不包含 Mock 数据、假设备、假 Key、假统计或只弹提示不请求服务端的演示按钮。

## 1. 使用方式

部署完成后打开：

```text
http://服务器IP:3000/admin/
```

Linux 一键部署完成时会直接显示真实管理员账号、工作区代码和随机初始密码，并保存到：

```text
/opt/universal-authorization/admin-login.txt
```

重新查看命令：

```bash
sudo cat /opt/universal-authorization/admin-login.txt
```

登录成功后，浏览器使用服务端签发的 HttpOnly Cookie 建立管理员会话；所有写操作都会携带当前会话的 CSRF 令牌。页面上的权限来自 `GET /admin/auth/me` 返回的真实权限列表。

## 2. 页面与真实功能

### 控制台

- 遍历全部分页读取产品、策略和 Key，不再只统计前 100 条。
- 显示产品总数、策略总数、Key 总数、有效 Key 和待激活 Key。
- 显示最近生成的真实 Key。
- `/health` 与 `/ready` 每 15 秒检查一次；服务状态不再写死为“在线”。
- API 失败会直接显示错误，不会用数字 0 冒充真实结果。

### 产品管理

完整对接产品、版本、功能定义的创建、读取和修改：

- 产品：名称、代码、说明、状态、最低版本、推荐版本、强制升级版本、Settings JSON。
- 版本：版本号、状态、强制升级、发布时间、发布说明。
- 功能：功能代码、名称、说明、状态。

### 授权策略

完整对接策略创建、分页读取和修改：

- `TRIAL` 试用授权。
- `DURATION` 时长授权。
- `FIXED_EXPIRY` 固定到期授权。
- `PERPETUAL` 永久授权。
- 最大设备、最大并发、离线宽限、自助解绑、解绑冷却、Rules JSON、状态。

### Key 管理

- 单枚 Key 真实生成。
- 最多 500 枚 Key 真实批量生成；需要 `licenses.export`。
- 明文 Key 仅展示本次生成响应，可复制或下载 TXT。
- 产品、策略和功能定义均从服务器读取。
- 功能授权支持“不写入、明确允许、明确拒绝”、Limits JSON 和单功能到期时间。
- 支持最大设备数和最大并发数覆盖、Metadata JSON。
- 支持产品、状态筛选和真实分页。
- 支持查看 Key 完整快照。
- 支持暂停、恢复、按时长续期、修改固定到期时间、永久吊销。

### 设备管理

设备功能位于 Key 详情中：

- 按绑定状态筛选并分页读取。
- 查看设备 UUID、激活 UUID、平台、系统版本、公钥指纹、指纹摘要、风险分、在线会话、首次和最近出现时间、封禁原因。
- 管理员强制解绑。
- 设备封禁和解封。
- 危险操作必须填写真实原因；永久吊销还需要文字确认。

### 审计与授权事件

管理员审计支持全部服务端筛选字段：

- 主体类型、主体 UUID、操作代码。
- 资源类型、资源 ID。
- 操作结果、请求 ID。
- 开始时间和结束时间。

授权事件支持全部服务端筛选字段：

- 产品、Key、设备、激活和会话 UUID。
- 事件类型、结果、原因代码、请求 ID。
- 开始时间和结束时间。

详情抽屉展示完整 UUID、来源 IP、User-Agent、变更前数据、变更后数据和 Metadata，不再只截取 UUID 前 8 位。

### API 中心

- 实时调用 `/health` 和 `/ready`。
- 可真实调用 `/admin/auth/csrf` 刷新当前页面 CSRF 令牌。
- 展示当前服务端全部 52 个 API 路由：7 个客户端接口、39 个管理业务接口、6 个认证与状态接口。
- 客户端激活、验证、心跳等接口不会在后台伪造调用，因为它们必须由持有真实 Ed25519 私钥的设备完成签名。

### 管理员、角色与个人中心

- 管理员真实分页、搜索、创建、编辑、启用、暂停和禁用。
- 分页响应返回真实 `total`，不是用当前页条数冒充总数。
- 新增管理员必须明确选择至少一个角色，不默认授予 `owner`。
- 当前管理员不能停用自己、修改自己的角色或在管理员列表重置自己的密码。
- 其他管理员密码重置后，全部旧会话立即失效。
- 个人中心支持修改显示名称和自己的登录密码；修改密码后强制重新登录。
- 平台管理员的个人中心不要求指定目标租户，只会修改当前登录账号。
- 自定义角色支持真实权限矩阵；系统 `owner` 角色不可修改。
- 只有 `admin.roles.manage` 的账号也能进入角色管理，不需要额外拥有管理员账号管理权限。

### 工作区设置

- 后台名称、支持邮箱、默认授权天数、默认设备上限和到期预警天数真实写入 PostgreSQL `system_settings`。
- 当前默认值尚未接入 Key 创建表单和通知任务，页面明确展示该边界，不伪装成已生效。

## 3. 权限行为

导航、页面和操作按钮均按以下真实权限控制：

```text
products.read
products.write
licenses.read
licenses.write
licenses.export
devices.read
devices.unbind
devices.block
audit.read
admin.users.manage
admin.roles.manage
tenant.settings.manage
```

没有读取权限时，页面不会继续发送越权请求；没有写权限时，创建、修改和危险操作按钮不会显示。

## 4. 接口覆盖数量

| 类别 | 数量 | 对接位置 |
|---|---:|---|
| 客户端授权接口 | 7 | API 中心完整目录；由真实客户端 SDK 调用 |
| 管理业务接口 | 39 | 产品、策略、Key、设备、审计、管理员、角色、个人中心和设置页面 |
| 管理员认证与状态接口 | 6 | 登录、会话初始化、用户菜单、API 中心和全局状态 |
| 合计 | 52 | `web/src/api/catalog.ts` |

接口目录有自动化测试，检查数量、重复项、UI 映射和管理权限说明。

## 5. Linux 更新

服务器上重新执行同一条安装命令即可拉取最新代码并重新构建：

```bash
curl -fsSL https://raw.githubusercontent.com/Jerry2586/Universal-authorization/main/install.sh | sudo bash
```

完成后访问并查看登录信息：

```bash
sudo cat /opt/universal-authorization/admin-login.txt
```

凭据文件会列出后台 URL、管理员账号、工作区代码和首次安装初始密码。

如果浏览器仍显示旧页面，执行强制刷新；Chrome/Edge 可使用 `Ctrl + F5`。

## 6. 第二至第四阶段真实可用性整改

- 所有主要列表页面均区分“真实空数据”和“接口请求失败”。
- 请求失败会显示服务端错误和可执行真实 refetch 的重新加载按钮。
- 产品、策略、Key、设备、审计日志和授权事件返回数据库真实 `total`。
- 前端分页使用真实总数并自动修正越界页。
- 新增和编辑操作在所需依赖接口失败时会被禁用，避免提交无效关联。
- Linux 一键部署必须真实访问 `/health`、`/ready` 和 `/admin/` 后才报告成功。
- 项目没有 Mock 列表、演示数据、假按钮或仅前端保存的业务功能。

完整设计和新手使用方法见 `17-管理后台成熟化第二至第四阶段设计与使用.md`。
