# 第九部分：一键部署设计与使用

> 本文只负责部署现有 `0.8.0` 授权服务器，不新增业务接口，也不修改第一至第八步授权逻辑。

## 1. 一键部署解决什么问题

以前需要手动复制配置、生成密钥、启动数据库、执行迁移、编译并启动服务。现在只运行一个脚本即可完成全部操作。

```text
运行 一键搭建.bat / deploy.ps1 / deploy.sh
        │
        ├─ 检查 Docker 和 Compose
        ├─ 自动生成或补全 .env
        │    ├─ PostgreSQL 随机密码
        │    ├─ 管理网关随机令牌
        │    ├─ License Key Pepper
        │    └─ Ed25519 服务端签名私钥
        ├─ 构建授权服务器镜像
        ├─ 启动 PostgreSQL 和 Redis
        ├─ 等待基础设施健康
        ├─ 自动执行 0001～0007 数据库迁移
        ├─ 启动授权服务器
        └─ 等待 /ready 健康检查通过
```

## 2. 文件说明

```text
Dockerfile                         授权服务器镜像构建文件
.dockerignore                      防止私钥、依赖和构建文件进入镜像上下文
compose.yaml                       PostgreSQL + Redis + 授权服务器编排
scripts/docker-entrypoint.sh       容器启动时先迁移数据库，再启动服务
scripts/create-production-env.mjs  自动生成安全生产配置
一键搭建.bat                       Windows 双击启动入口
deploy.ps1                         Windows PowerShell 一键部署入口
deploy.sh                          Linux 一键部署入口
```

## 3. Windows 傻瓜式使用

### 第一步：安装软件

安装并启动 Docker Desktop。确认 Docker Desktop 正在运行。

### 第二步：双击运行

直接双击源码目录中的：

```text
一键搭建.bat
```

也可以在源码目录打开 PowerShell：

```powershell
powershell -ExecutionPolicy Bypass -File .\deploy.ps1
```

### 第三步：看到成功提示

```text
通用 Key 授权服务器搭建成功
健康检查：http://127.0.0.1:3000/health
就绪检查：http://127.0.0.1:3000/ready
```

## 4. Linux 服务器远程一键拉取安装

这是 Linux 测试服务器的主要安装方式。SSH 登录服务器后执行：

```bash
curl -fsSL https://raw.githubusercontent.com/Jerry2586/Universal-authorization/main/install.sh | sudo bash
```

没有 `curl` 时执行：

```bash
wget -qO- https://raw.githubusercontent.com/Jerry2586/Universal-authorization/main/install.sh | sudo bash
```

默认安装目录：

```text
/opt/universal-authorization
```

安装器会自动完成：

1. 识别 Linux 系统并安装 Git、curl 和 CA 证书。
2. 测试服务器没有 Docker 时，调用 Docker 官方安装脚本安装 Docker Engine 和 Compose。
3. 启动 Docker 服务并设置为开机启动。
4. 从 GitHub 拉取 `main` 分支最新源码。
5. 调用项目内的 `deploy.sh` 完成配置、构建、数据库迁移和健康检查。
6. 输出安装目录、本机地址、服务器局域网地址和日志命令。

自定义安装目录：

```bash
curl -fsSL https://raw.githubusercontent.com/Jerry2586/Universal-authorization/main/install.sh \
  | sudo bash -s -- --dir /data/universal-authorization
```

指定其他分支：

```bash
curl -fsSL https://raw.githubusercontent.com/Jerry2586/Universal-authorization/main/install.sh \
  | sudo bash -s -- --branch main
```

服务器已经安装 Docker，且不允许安装器修改 Docker 时：

```bash
curl -fsSL https://raw.githubusercontent.com/Jerry2586/Universal-authorization/main/install.sh \
  | sudo bash -s -- --skip-docker
```

重新执行同一条安装命令会使用 `git merge --ff-only` 安全更新源码，不会删除 `.env` 和 Docker 数据卷。安装目录存在未提交源码改动时，安装器会停止，不会强制覆盖。

如果源码已经下载到服务器，可以直接执行：

```bash
chmod +x deploy.sh
./deploy.sh
```

## 5. 脚本如何保护已有配置

- 第一次运行：根据 `.env.example` 创建 `.env`，并自动生成全部敏感值。
- 再次运行：保留已有非空安全值，不会每次更换数据库密码、Pepper 或签名私钥。
- 已有 `.env` 中的空值会自动补全。
- 如果已有 `.env` 仍使用示例占位符或默认数据库密码，脚本会显示安全警告，但不会擅自更换，避免导致已有 Key 摘要或数据库连接失效。
- `.env` 不会被复制进 Docker 镜像，也不会被 Git 上传。
- PostgreSQL 和 Redis 只绑定到本机 `127.0.0.1`，不会默认暴露给公网。

## 6. 数据保存在哪里

数据库和 Redis 数据存放在 Docker 命名卷中：

```text
license-postgres-data
license-redis-data
```

执行下面命令只停止容器，不删除数据：

```bash
docker compose down
```

只有明确要删除全部授权数据时，才可以执行：

```bash
docker compose down -v
```

> 警告：`-v` 会永久删除 PostgreSQL 和 Redis 数据，正常维护时不要使用。

## 7. 日常管理命令

```bash
# 查看全部容器状态
docker compose ps

# 查看授权服务日志
docker compose logs -f app

# 查看数据库日志
docker compose logs -f postgres

# 查看 Redis 日志
docker compose logs -f redis

# 重启授权服务器
docker compose restart app

# 修改源码后重新构建并启动
docker compose up -d --build

# 检查数据库迁移状态
docker compose exec app pnpm db:status
```

## 8. 修改端口

打开 `.env`：

```text
PORT=3000
```

例如改为：

```text
PORT=8080
```

然后重新执行：

```bash
docker compose up -d
```

访问地址变为：

```text
http://127.0.0.1:8080/health
```

## 9. 部署失败怎么办

先查看容器状态：

```bash
docker compose ps
```

再查看最近日志：

```bash
docker compose logs --tail 120 app
```

常见原因：

1. Docker Desktop 没有启动。
2. `3000`、`5432` 或 `6379` 端口已被其他程序占用。
3. 首次构建时网络无法下载 Node.js、PostgreSQL 或 Redis 镜像。
4. 人工修改 `.env` 后产生了不合法配置。

## 10. 云服务器网络设置

默认授权服务端口是 TCP `3000`。需要在云厂商安全组或服务器防火墙中放行该端口，客户端才能访问。

```text
允许公网访问：3000/TCP
只允许本机访问：5432/TCP（PostgreSQL）
只允许本机访问：6379/TCP（Redis）
```

PostgreSQL 和 Redis 已在 `compose.yaml` 中绑定到 `127.0.0.1`，不要额外将它们转发到公网。

## 11. 安全注意事项

- 不要上传 `.env`。
- 不要把 `LICENSE_SIGNING_PRIVATE_KEY_PEM_BASE64` 发给客户端。
- 不要公开 `MANAGEMENT_GATEWAY_TOKEN`。
- 正式公网部署时，应在授权服务器前配置 HTTPS 反向代理。
- 正式生产环境应定期备份 PostgreSQL 数据卷。
- 不要随意删除或重新生成签名私钥，否则旧令牌和设备证书可能无法继续验签。
