import { useState, type FormEvent } from 'react';
import { Navigate } from 'react-router-dom';
import { ArrowRight, Eye, EyeOff, KeyRound, LockKeyhole, ShieldCheck, Sparkles } from 'lucide-react';
import { useAuth } from '../auth/AuthProvider';
import { ApiError } from '../api/client';
export function LoginPage(){const {status,login}=useAuth(); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [tenant,setTenant]=useState('default'); const [show,setShow]=useState(false); const [busy,setBusy]=useState(false); const [error,setError]=useState(''); if(status==='authenticated')return <Navigate to="/" replace/>; async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{await login({email,password,...(tenant.trim()?{tenant_code:tenant.trim()}: {})})}catch(err){setError(err instanceof ApiError?err.message:'登录失败，请稍后重试')}finally{setBusy(false)}} return <div className="login-page"><div className="login-aurora one"/><div className="login-aurora two"/><section className="login-story"><div className="story-brand"><div className="logo-glyph large"><span/></div><div><strong>Universal Authorization</strong><small>SECURE LICENSE INFRASTRUCTURE</small></div></div><div className="story-copy"><span className="eyebrow"><Sparkles/> 新一代授权控制台</span><h1>让每一枚 Key<br/><em>清晰、可控、可信。</em></h1><p>从产品策略到设备绑定，在一个安全、现代且高效的工作台中完成授权全生命周期管理。</p><div className="trust-grid"><div><ShieldCheck/><span><strong>安全会话</strong><small>HttpOnly + CSRF</small></span></div><div><KeyRound/><span><strong>Key 全周期</strong><small>生成 · 续期 · 吊销</small></span></div></div></div><div className="story-status"><i/><span>Authorization Core</span><strong>Operational</strong></div></section><section className="login-panel"><div className="login-card"><div className="login-heading"><span><LockKeyhole/></span><div><h2>欢迎回来</h2><p>登录您的授权管理工作区</p></div></div><form onSubmit={submit}><label>管理员邮箱<input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="admin@example.com" autoComplete="username" required/></label><label>工作区代码<input value={tenant} onChange={e=>setTenant(e.target.value)} placeholder="default" autoCapitalize="none"/></label><label>密码<div className="password-field"><input type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} placeholder="输入管理员密码" autoComplete="current-password" minLength={8} required/><button type="button" onClick={()=>setShow(v=>!v)}>{show?<EyeOff/>:<Eye/>}</button></div></label>{error&&<div className="form-error">{error}</div>}<button className="primary login-submit" disabled={busy}>{busy?<><span className="spinner"/>正在验证...</>:<>安全登录<ArrowRight/></>}</button></form><div className="login-note"><ShieldCheck/>登录信息仅发送到当前授权服务器，管理网关密钥不会暴露给浏览器。</div></div></section></div>}
