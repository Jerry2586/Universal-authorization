# 第五步：在线验证与授权令牌刷新设计

> 当前服务版本：`0.5.0`  
> 本文只说明第五步。第五步只开发“在线验证”和“令牌刷新”，不开发心跳、会话释放、设备解绑或管理后台页面。

---

## 1. 这一部到底做什么

第四步已经让客户端拿到了两样东西：

1. `device_certificate`：证明这台设备已经登记并绑定过。
2. `license_token`：证明当前短期授权会话可以使用。

第五步解决两个问题：

- 客户端想知道授权现在是否仍然有效，调用 `POST /api/v1/licenses/verify`。
- 短期授权令牌快到期，客户端调用 `POST /api/v1/licenses/refresh` 换一个新令牌。

可以把它理解成：

```text
第四步 = 第一次领证
第五步验证 = 每次联网查证件是否仍有效
第五步刷新 = 旧临时通行证换新临时通行证
```

---

## 2. 第五步严格边界

### 2.1 本步已经实现

```text
POST /api/v1/licenses/verify
POST /api/v1/licenses/refresh
```

具体能力：

- 验证服务端签发的 Ed25519 设备凭证。
- 验证服务端签发的 Ed25519 授权令牌。
- 检查令牌的 `kid`、`typ`、签名、签发方、产品、开始时间和到期时间。
- 检查设备凭证与授权令牌是否属于同一租户、Key、设备和绑定。
- 使用数据库保存的设备公钥验证本次请求签名。
- 检查请求时间窗口。
- 使用 Redis 原子 `SET NX EX` 防止相同随机数重放。
- 实时检查租户、产品、客户端版本、Key、设备、绑定和数据库会话。
- 返回数据库里的最新功能权限。
- 刷新时撤销旧会话并创建新会话。
- 刷新不修改 Key 的业务到期时间和设备绑定。
- 刷新使用 PostgreSQL 幂等记录。
- 写入 `LICENSE_VERIFIED` 和 `LICENSE_TOKEN_REFRESHED` 授权事件。

### 2.2 本步明确没有实现

```text
POST /api/v1/sessions/heartbeat
POST /api/v1/sessions/release
POST /api/v1/devices/unbind
```

也没有实现：

- Redis 在线会话集合。
- 管理员设备解绑和封禁接口。
- 支付、订单、代理商系统。
- 管理后台网页。

这些地址现在返回 `404 ROUTE_NOT_FOUND`，这是故意保留的阶段边界，不是漏写。

---

## 3. 模块架构图

```text
客户端
  │
  │ HTTPS + 设备 Ed25519 请求签名
  ▼
Fastify 路由
  ├─ POST /api/v1/licenses/verify
  └─ POST /api/v1/licenses/refresh
  │
  ▼
DeviceRequestAuthenticator（统一请求认证器）
  ├─ CompactTokenVerifier
  │    ├─ 验设备凭证 UDC+JWT
  │    └─ 验授权令牌 ULT+JWT
  ├─ PostgreSQL 读取已登记设备公钥
  ├─ Ed25519DeviceSignatureVerifier 验本次请求签名
  └─ RequestReplayStore 使用 Redis 防重放
  │
  ├───────────────────────────────┐
  ▼                               ▼
LicenseVerificationService     LicenseRefreshService
  │                               ├─ PostgreSQL 刷新幂等
  │                               ├─ 撤销旧会话
  │                               ├─ 创建新会话
  │                               └─ CompactTokenIssuer 签新令牌
  └───────────────┬───────────────┘
                  ▼
        LicenseRuntimeRepository
          ├─ PostgreSQL 实时状态
          ├─ 产品和版本策略
          ├─ Key 业务有效期
          ├─ 设备/绑定/会话状态
          ├─ 最新功能权限
          └─ 授权事件
```

### 3.1 为什么拆成这些模块

- 路由只负责收请求，不写业务判断。
- 认证器只负责“你是谁、请求是不是你签的、有没有重放”。
- Service 只负责“验证”或“刷新”的业务流程。
- Repository 只负责数据库读取和事务。
- Redis 只负责短时间防重放，不保存业务最终状态。
- PostgreSQL 是授权状态的最终裁决方。

---

## 4. 在线验证接口

### 4.1 地址

```http
POST /api/v1/licenses/verify
Content-Type: application/json
```

### 4.2 请求头

```http
X-Product-Code: demo-product
X-Client-Version: 1.2.0
X-Timestamp: 2026-08-24T12:05:00.000Z
X-Client-Nonce: verify-client-nonce-123456
X-Device-Id: 设备UUID
X-Key-Id: device-key-v1
X-Signature: 设备私钥生成的Base64URL签名
```

注意：这里的 `X-Key-Id` 是设备密钥编号，不是授权 Key 明文。

### 4.3 请求体

```json
{
  "device_certificate": "第四步返回的设备凭证",
  "license_token": "第四步返回的授权令牌",
  "client_version": "1.2.0",
  "timestamp": "2026-08-24T12:05:00.000Z",
  "client_nonce": "verify-client-nonce-123456"
}
```

### 4.4 成功响应示例

```json
{
  "request_id": "请求编号",
  "success": true,
  "code": "OK",
  "message": "授权在线验证通过",
  "server_time": "2026-08-24T12:05:00.000Z",
  "data": {
    "valid": true,
    "license_id": "授权UUID",
    "device_id": "设备UUID",
    "activation_id": "绑定UUID",
    "session_id": "会话UUID",
    "license_status": "ACTIVE",
    "session_status": "VALID",
    "verified_at": "2026-08-24T12:05:00.000Z",
    "expires_at": "2026-08-24T12:15:00.000Z",
    "license_expires_at": null,
    "offline_until": "2026-08-25T12:15:00.000Z",
    "should_refresh": false,
    "refresh_after_seconds": 600,
    "features": [
      {
        "code": "api.access",
        "allowed": true,
        "limits": { "rpm": 60 },
        "expires_at": null
      }
    ]
  }
}
```

### 4.5 验证顺序

服务器严格按下面的思路处理：

```text
1. 时间戳是否在允许窗口内
2. 设备凭证是不是服务端签的
3. 授权令牌是不是服务端签的
4. 两个令牌是不是同一套授权上下文
5. 数据库能否找到这台设备登记的公钥
6. 本次请求是不是由该设备私钥签名
7. Redis 中这个设备随机数是不是第一次出现
8. 租户和产品是否启用
9. 客户端版本是否允许
10. Key 是否为 ACTIVE 且未到期
11. 设备是否有效、是否被封禁
12. 绑定是否仍为 ACTIVE
13. 数据库会话是否存在、未撤销、未过期
14. 返回数据库最新功能权限
```

客户端不能只在本地读取令牌里的 `license_status` 就认为永远有效，因为管理员可能已经在数据库冻结或吊销 Key。

---

## 5. 令牌刷新接口

### 5.1 地址

```http
POST /api/v1/licenses/refresh
Content-Type: application/json
```

### 5.2 请求头

刷新接口使用验证接口的全部请求头，另外增加：

```http
Idempotency-Key: refresh-idempotency-123456
```

### 5.3 请求体

```json
{
  "device_certificate": "第四步返回的设备凭证",
  "license_token": "当前授权令牌",
  "client_version": "1.2.0",
  "timestamp": "2026-08-24T12:10:00.000Z",
  "client_nonce": "refresh-client-nonce-123456",
  "idempotency_key": "refresh-idempotency-123456"
}
```

请求头和请求体的幂等键必须完全一样。

### 5.4 刷新成功后发生什么

```text
旧会话 VALID/GRACE
       │
       ├─ 改成 REVOKED
       │
       └─ 创建新会话 VALID
              │
              └─ 签发新的短期 license_token
```

不发生下面这些事情：

- 不延长 `license_keys.expires_at`。
- 不创建新的设备。
- 不创建新的设备绑定。
- 不改变授权类型。
- 不绕过设备数和并发数限制。

### 5.5 成功响应示例

```json
{
  "success": true,
  "code": "OK",
  "message": "授权令牌刷新成功",
  "data": {
    "license_id": "授权UUID",
    "device_id": "设备UUID",
    "activation_id": "绑定UUID",
    "previous_session_id": "旧会话UUID",
    "session_id": "新会话UUID",
    "license_status": "ACTIVE",
    "session_status": "VALID",
    "issued_at": "2026-08-24T12:10:00.000Z",
    "expires_at": "2026-08-24T12:25:00.000Z",
    "license_expires_at": null,
    "offline_until": "2026-08-25T12:25:00.000Z",
    "heartbeat_interval_seconds": 60,
    "refresh_after_seconds": 600,
    "features": [],
    "license_token": "新的短期授权令牌",
    "signing_key_id": "license-signing-v1"
  }
}
```

客户端收到成功响应后，应原子替换本地旧 `license_token`。设备凭证不需要每次刷新都更换。

---

## 6. 设备请求怎么签名

验证和刷新使用同一套规范化格式：

```text
v1
POST
请求路径
产品编码
客户端版本
时间戳
客户端随机数
设备ID
设备密钥编号
幂等键或空字符串
请求体规范JSON的SHA-256
```

验证请求的路径：

```text
/api/v1/licenses/verify
```

验证请求没有幂等键，所以对应行是空字符串。

刷新请求的路径：

```text
/api/v1/licenses/refresh
```

刷新请求必须把 `idempotency_key` 放入签名内容。

最终使用设备 Ed25519 私钥对上述完整 UTF-8 文本签名，再把 64 字节签名编码成 Base64URL 放进 `X-Signature`。

傻瓜式注意事项：

1. JSON 字段或值只要改一个字符，签名就会失败。
2. 请求头时间戳、随机数、客户端版本必须与请求体一致。
3. 验证路径和刷新路径不能混用签名。
4. 设备私钥永远只保存在客户端本机。

---

## 7. 两层防重放

### 7.1 普通在线验证

Redis 键：

```text
license-server:request-replay:<deviceId>:<clientNonce>
```

写入方法：

```text
SET key requestHash NX EX 300
```

- 第一次成功。
- TTL 内第二次使用相同设备随机数，返回 `REPLAY_DETECTED`。

### 7.2 令牌刷新

刷新除了 Redis 随机数，还使用 PostgreSQL 幂等表：

```text
license_refresh_idempotency_records
```

规则：

- 同一幂等键 + 完全相同请求：返回第一次的同一刷新结果。
- 同一幂等键 + 不同请求：返回 `IDEMPOTENCY_CONFLICT`。
- 第一次还在处理：返回 `REQUEST_IN_PROGRESS`。

为什么要两层：

- Redis 随机数防止签名请求被快速复制重放。
- PostgreSQL 幂等防止客户端超时重试时重复创建新会话。

---

## 8. 紧凑令牌验签规则

设备凭证类型：

```text
UDC+JWT
```

授权令牌类型：

```text
ULT+JWT
```

服务器检查：

- 必须是三段式紧凑格式。
- Header `alg` 必须为 `EdDSA`。
- Header `typ` 必须匹配令牌用途。
- Header `kid` 必须能在验证密钥列表中找到。
- Ed25519 签名必须正确。
- Claim `signing_key_id` 必须与 `kid` 一致。
- `iss` 必须等于配置的签发方。
- `aud` 必须等于 `X-Product-Code`。
- `nbf` 不能晚于当前时间。
- `exp` 必须晚于当前时间。
- 必要 UUID 和 Claim 类型必须完整。

---

## 9. 实时数据库检查表

| 对象 | 允许状态 | 拒绝例子 |
|---|---|---|
| 租户 | `ACTIVE` | `TENANT_DISABLED` |
| 产品 | `ACTIVE` | `PRODUCT_DISABLED` |
| 客户端版本 | 未封禁并满足最低版本 | `CLIENT_VERSION_BLOCKED` |
| Key | `ACTIVE` 且在业务有效期内 | `LICENSE_SUSPENDED`、`LICENSE_REVOKED`、`LICENSE_EXPIRED` |
| 设备 | `ACTIVE` 且不在封禁表 | `DEVICE_BLOCKED` |
| 绑定 | `ACTIVE` | `DEVICE_UNBOUND` |
| 会话 | `VALID` 或 `GRACE` 且未过期 | `SESSION_REVOKED`、`SESSION_EXPIRED` |

令牌内的功能权限只是签发时快照。在线验证和刷新返回的是 PostgreSQL 当前最新功能授权。

---

## 10. 常见错误码

| 错误码 | 人话解释 |
|---|---|
| `DEVICE_CREDENTIAL_INVALID` | 设备凭证格式、签名、产品或登记信息不对 |
| `DEVICE_CREDENTIAL_EXPIRED` | 设备凭证过期，需要重新激活 |
| `LICENSE_TOKEN_INVALID` | 授权令牌格式或签名不对 |
| `LICENSE_TOKEN_EXPIRED` | 当前短期授权令牌已过期 |
| `TOKEN_CONTEXT_MISMATCH` | 设备凭证和授权令牌不是同一套 |
| `SIGNATURE_INVALID` | 本次请求不是登记设备私钥签的 |
| `REQUEST_EXPIRED` | 客户端时间与服务器相差太大 |
| `REPLAY_DETECTED` | 设备随机数已经使用过 |
| `SESSION_NOT_FOUND` | 数据库找不到令牌对应会话 |
| `SESSION_REVOKED` | 会话已经撤销 |
| `SESSION_EXPIRED` | 数据库会话已经到期 |
| `LICENSE_SUSPENDED` | Key 被冻结 |
| `LICENSE_REVOKED` | Key 被永久吊销 |
| `LICENSE_EXPIRED` | Key 业务期限到期 |
| `DEVICE_BLOCKED` | 设备或绑定被封禁 |
| `DEVICE_UNBOUND` | 设备绑定已解绑或失效 |
| `IDEMPOTENCY_CONFLICT` | 同一幂等键被用于不同刷新请求 |
| `REQUEST_IN_PROGRESS` | 相同刷新还在处理中 |

---

## 11. 数据库迁移

本步只新增：

```text
database/migrations/0004_verification_refresh_stage.sql
```

新增表：

```text
license_refresh_idempotency_records
```

没有修改：

```text
0001_initial_schema.sql
0002_management_stage.sql
0003_activation_stage.sql
```

运行：

```powershell
pnpm db:migrate
pnpm db:status
```

---

## 12. 配置说明

```env
LICENSE_REQUEST_MAX_SKEW_SECONDS=300
REQUEST_REPLAY_TTL_SECONDS=300
LICENSE_REFRESH_IDEMPOTENCY_TTL_SECONDS=86400
```

人话解释：

- `LICENSE_REQUEST_MAX_SKEW_SECONDS`：客户端时间最多允许与服务器相差多少秒。
- `REQUEST_REPLAY_TTL_SECONDS`：相同设备随机数在 Redis 中锁多久。
- `LICENSE_REFRESH_IDEMPOTENCY_TTL_SECONDS`：刷新结果允许安全重试多久。

继续复用第四步配置：

```env
LICENSE_TOKEN_TTL_SECONDS=900
LICENSE_TOKEN_ISSUER=universal-license-server
HEARTBEAT_INTERVAL_SECONDS=60
REFRESH_AFTER_SECONDS=600
```

`HEARTBEAT_INTERVAL_SECONDS` 目前只是返回给客户端的协议参数，本步仍未实现心跳接口。

---

## 13. 傻瓜式运行步骤

### 第 1 步：更新本地配置

把 `.env.example` 中第五步的三个配置复制进 `.env`。

### 第 2 步：确认签名私钥已配置

```env
LICENSE_SIGNING_PRIVATE_KEY_PEM_BASE64=你的Base64私钥
```

如果没有，运行：

```powershell
pnpm signing-key:generate
```

### 第 3 步：启动 PostgreSQL 和 Redis

```powershell
pnpm infra:up
```

### 第 4 步：执行迁移

```powershell
pnpm db:migrate
```

### 第 5 步：启动服务

```powershell
pnpm dev
```

### 第 6 步：跑完整检查

```powershell
pnpm typecheck
pnpm test
pnpm build
```

---

## 14. 主要代码位置

```text
src/modules/cryptography/compact-token-verifier.ts
src/modules/cryptography/ed25519-device-signature-verifier.ts

src/modules/security/request-replay.store.ts
src/modules/security/infrastructure/redis-request-replay.store.ts

src/modules/verification/device-request-authenticator.ts
src/modules/verification/license-verification.service.ts
src/modules/verification/license-runtime.repository.ts
src/modules/verification/verification.routes.ts
src/modules/verification/infrastructure/postgres-license-runtime.repository.ts

src/modules/refresh/license-refresh.service.ts
src/modules/refresh/license-refresh-idempotency.store.ts
src/modules/refresh/infrastructure/postgres-license-refresh-idempotency.store.ts
```

---

## 15. 第五步完成标准

满足下面全部条件才算第五步完成：

- [x] 有中文傻瓜式设计文档。
- [x] 设备凭证可验签。
- [x] 授权令牌可验签。
- [x] 已绑定设备请求可验签。
- [x] 请求时间窗口已检查。
- [x] Redis 防重放已实现。
- [x] 在线验证实时读取 PostgreSQL 状态。
- [x] 刷新撤销旧会话并创建新会话。
- [x] 刷新不改变 Key 业务到期时间。
- [x] 刷新幂等已实现。
- [x] 最新功能权限会返回客户端。
- [x] 第五步之外三个接口仍为 404。

---

## 16. 下一步边界

第五步到此结束。没有明确收到第六步命令前，不开发：

```text
POST /api/v1/sessions/heartbeat
POST /api/v1/sessions/release
POST /api/v1/devices/unbind
```
