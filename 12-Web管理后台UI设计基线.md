# Web 管理后台《UI设计基线》

> 基线编号：UIB-001
> 项目：Universal Authorization / 通用 Key 授权服务器
> 项目版本：0.8.0
> 设计来源：后台 UI-01 为全局主方案，后台 UI-03 为 Key 管理页面专项方案
> 生成日期：2026-08-24
> 当前状态：待用户确认；确认后冻结
> 当前人格：2号人格（资深 UI/UX 设计工程师）

---

## 1. 用户选定方案

用户已经选定：

```text
以后台UI-01为主，采用后台UI-03的Key管理页面
```

最终组合规则：

1. **整个管理后台的外壳、品牌感、导航、颜色和公共组件以 UI-01 为准。**
2. **Key 管理页面的高密度筛选区、数据表格、详情抽屉和一次性交付提示以 UI-03 为准。**
3. UI-03 原图中的混合导航不带入最终后台；Key 页面仍使用 UI-01 的左侧导航和顶部栏，避免同一系统出现两套导航逻辑。
4. 原效果图中的演示数字仅用于视觉构图，正式页面不得伪造统计数据。

---

## 2. 设计目标

| 编号 | 目标 | 验收标准 |
|---|---|---|
| UI-BASE-001 | 普通网站式后台体验 | 管理员通过登录页进入统一后台框架 |
| UI-BASE-002 | 专业可信 | 界面克制、清晰，不使用游戏化或夸张霓虹效果 |
| UI-BASE-003 | 高频操作高效率 | 产品、策略、Key 和设备操作不超过合理点击层级 |
| UI-BASE-004 | 权限透明 | 无权限菜单和按钮必须隐藏或明确禁用 |
| UI-BASE-005 | 安全操作 | Key 明文一次性交付，吊销/解绑/封禁需要确认 |
| UI-BASE-006 | 真实数据 | 不展示后端无法提供的假数据和假按钮 |
| UI-BASE-007 | Linux 可部署 | 最终后台可由现有一键部署脚本同域部署 |

---

## 3. 全局视觉方向

### 3.1 风格关键词

```text
简洁 / 商务 / 科技 / 稳定 / 可信 / 高效 / 中等信息密度
```

禁止方向：

- 过度霓虹。
- 游戏化控制台。
- 大面积渐变背景。
- 玻璃拟态导致可读性下降。
- 纯装饰性 3D 图形。
- 与授权业务无关的大幅插画。

### 3.2 颜色基线

| Token | 建议颜色 | 用途 |
|---|---|---|
| `color-primary-600` | `#2563EB` | 主按钮、选中项、链接 |
| `color-primary-700` | `#1D4ED8` | 主按钮悬停 |
| `color-sidebar` | `#172033` | 左侧导航背景 |
| `color-sidebar-hover` | `#24314A` | 导航悬停 |
| `color-page-bg` | `#F4F7FB` | 页面背景 |
| `color-surface` | `#FFFFFF` | 卡片、表格、弹窗 |
| `color-text-primary` | `#172033` | 主文字 |
| `color-text-secondary` | `#64748B` | 次要文字 |
| `color-border` | `#E2E8F0` | 分割线、输入框边框 |
| `color-success` | `#16A34A` | 正常、ACTIVE、SUCCESS |
| `color-warning` | `#D97706` | 即将到期、风险提醒 |
| `color-danger` | `#DC2626` | FAILURE、REVOKED、永久操作 |
| `color-info` | `#0284C7` | 信息提示、CREATED |
| `color-disabled` | `#94A3B8` | DISABLED、不可用操作 |

颜色不能作为唯一状态表达方式，状态必须同时显示文字标签。

### 3.3 状态颜色映射

#### Key 状态

| 状态 | 中文 | 颜色 |
|---|---|---|
| `CREATED` | 未激活 | 信息蓝 |
| `ACTIVE` | 正常 | 成功绿 |
| `SUSPENDED` | 已暂停 | 警告橙 |
| `EXPIRED` | 已过期 | 中性灰 |
| `REVOKED` | 已永久吊销 | 危险红 |
| `DISABLED` | 已禁用 | 深灰 |

#### 产品和功能状态

| 状态 | 中文 | 颜色 |
|---|---|---|
| `ACTIVE` | 已启用 | 成功绿 |
| `DISABLED` | 已停用 | 中性灰 |

#### 产品版本状态

| 状态 | 中文 | 颜色 |
|---|---|---|
| `ACTIVE` | 正常 | 成功绿 |
| `BLOCKED` | 已阻止 | 危险红 |
| `DEPRECATED` | 已弃用 | 警告橙 |

#### 事件结果

| 状态 | 中文 | 颜色 |
|---|---|---|
| `SUCCESS` | 成功 | 成功绿 |
| `FAILURE` | 失败 | 危险红 |

### 3.4 字体基线

中文优先字体栈：

```css
"Inter", "PingFang SC", "Microsoft YaHei", "Noto Sans CJK SC", sans-serif
```

| 场景 | 字号 | 字重 |
|---|---:|---:|
| 页面标题 | 24px | 600 |
| 区块标题 | 18px | 600 |
| 卡片数字 | 26～30px | 700 |
| 正文/表格 | 14px | 400 |
| 按钮 | 14px | 500 |
| 辅助文字 | 12px | 400 |

禁止使用小于 12px 的业务文字。

### 3.5 间距和圆角

使用 4px 基础网格：

```text
4 / 8 / 12 / 16 / 20 / 24 / 32 / 40
```

| 元素 | 基线 |
|---|---|
| 页面外边距 | 桌面 24px；平板 16px；手机 12px |
| 卡片内边距 | 20～24px |
| 表单字段间距 | 16px |
| 卡片间距 | 16px |
| 普通圆角 | 8px |
| 卡片圆角 | 10px |
| 弹窗圆角 | 12px |
| 控件高度 | 普通 36px；重要表单 40px |

---

## 4. 全局布局基线

### 4.1 桌面布局

```text
┌──────────────────────────────────────────────────────────┐
│ 左侧导航 240px │ 顶部栏 64px                            │
│                ├─────────────────────────────────────────┤
│                │ 面包屑 + 页面标题 + 页面级操作          │
│                ├─────────────────────────────────────────┤
│                │ 页面内容区                              │
│                │ 卡片 / 筛选 / 表格 / 图表 / 空状态       │
└──────────────────────────────────────────────────────────┘
```

- 左侧导航固定宽度 240px，可折叠为 72px。
- 顶部栏高度 64px，固定在内容区顶部。
- 内容最大宽度不强制锁死，数据表格优先使用可用宽度。
- 页面标题区与内容区分离。

### 4.2 左侧导航顺序

```text
Universal Authorization

后台首页
产品管理
授权策略
Key 管理
设备管理
审计日志
授权事件
接口中心

v0.8.0
```

规则：

- 只显示当前管理员有权访问的菜单。
- “设备管理”第一期如果没有全局设备 API，入口进入说明页或从 Key 详情进入；不得显示虚假的全局设备列表。
- 不显示租户管理、管理员管理、角色管理、系统设置和签名密钥管理，直到后端 API 完成。

### 4.3 顶部栏

从左到右：

1. 移动端/折叠菜单按钮。
2. 面包屑。
3. 弹性空白。
4. 服务状态。
5. 刷新按钮。
6. 管理员头像、名称和退出入口。
7. 平台管理员租户上下文选择器仅在租户列表接口完成后显示；接口未完成时不得制作假下拉列表。

---

## 5. 公共组件基线

| 组件编号 | 组件 | 规则 |
|---|---|---|
| UI-COMP-001 | 侧边导航 | 图标+文字；选中项科技蓝背景或左侧标记 |
| UI-COMP-002 | 顶部栏 | 面包屑、服务状态、刷新、管理员菜单 |
| UI-COMP-003 | 页面标题栏 | 标题、说明、最多两个主要操作 |
| UI-COMP-004 | 状态标签 | 颜色+中文文字，不只显示颜色 |
| UI-COMP-005 | 数据表格 | 固定表头、悬停行、空状态、加载骨架 |
| UI-COMP-006 | 筛选栏 | 桌面单行；窄屏自动换行 |
| UI-COMP-007 | 表单抽屉 | 用于普通创建和编辑，宽 520～640px |
| UI-COMP-008 | 详情抽屉 | 用于 Key、审计和事件详情，宽 640～760px |
| UI-COMP-009 | 普通确认弹窗 | 暂停、恢复等可恢复操作 |
| UI-COMP-010 | 危险确认弹窗 | 永久吊销、解绑、封禁；必须说明影响 |
| UI-COMP-011 | JSON 编辑器 | 格式化、语法校验、错误行提示 |
| UI-COMP-012 | 空状态 | 说明无数据原因；有权限时提供真实创建入口 |
| UI-COMP-013 | 错误状态 | 显示中文信息和 request_id，允许复制追踪号 |
| UI-COMP-014 | 权限状态 | 403 页面或禁用说明，不伪装成系统错误 |
| UI-COMP-015 | Toast 通知 | 成功简短提示；失败保留足够阅读时间 |
| UI-COMP-016 | 一次性交付面板 | Key 明文复制、下载、安全保存确认 |

---

## 6. 页面设计基线

### PAGE-001 登录页

视觉沿用 UI-01：浅灰背景、居中白色登录卡片、科技蓝主按钮。

内容：

- 系统名称。
- 管理员邮箱。
- 密码。
- 登录按钮。
- 登录中状态。
- 通用错误提示。
- 服务不可用提示。

不在本期凭空加入：注册、找回密码、短信登录、第三方登录、MFA 输入框。对应后端能力完成后另立变更单。

### PAGE-002 后台首页

布局以 UI-01 为准：

1. 服务状态卡片：授权服务、PostgreSQL、Redis、版本。
2. 业务概览区。
3. Key 状态区。
4. 最近授权事件。
5. 快捷操作：新建产品、生成 Key、批量生成 Key。

数据规则：

- `/health` 和 `/ready` 可直接显示真实状态。
- 后端统计接口未完成前，不显示伪造的产品总数、Key 总数和图表数字。
- 可用“统计接口待启用”占位卡，或只展示当前接口可以真实提供的数据。
- 最近授权事件来自 `/admin/v1/license-events`。

### PAGE-003 产品列表

- 中等密度表格。
- 列：产品编码、名称、状态、最低版本、推荐版本、强制更新版本、更新时间、操作。
- 主要操作：新建产品。
- 行操作：查看详情、编辑。
- 不提供删除按钮。
- 当前接口没有搜索和状态筛选，第一期不得放无法工作的筛选控件。

### PAGE-004 产品详情

使用标签页：

```text
基本资料 | 产品版本 | 功能定义
```

- 基本资料：产品信息和版本规则。
- 产品版本：版本列表、创建、编辑。
- 功能定义：功能列表、创建、编辑。
- 不提供删除版本、删除功能。

### PAGE-005 授权策略

- 策略列表为主。
- 创建/编辑使用右侧抽屉。
- 授权类型切换后动态显示时长或固定到期规则。
- 普通字段可视化；`rules` 放入“高级设置”。
- 不提供删除和复制按钮。

### PAGE-006 Key 管理

本页面采用 UI-03 的高密度数据工作台，但保留 UI-01 的全局外壳。

#### 顶部区域

```text
页面标题：Key 管理
说明：创建、查询并管理授权 Key 生命周期
主要操作：[生成 Key] [批量生成 Key]
```

#### 状态概览条

状态顺序固定：

```text
未激活 / 正常 / 已暂停 / 已过期 / 已永久吊销 / 已禁用
```

- 只有统计接口完成后才显示真实数量。
- 无统计接口时状态概览条可以隐藏，不得通过当前页数据冒充全局总数。

#### 筛选栏

第一期只允许：

- 产品。
- Key 状态。
- 每页数量。
- 查询。
- 重置。

禁止在后端未支持前加入：

- Key 关键字搜索。
- 批次搜索。
- 到期时间范围。
- 创建人筛选。

#### Key 表格

固定列：

| 顺序 | 列 |
|---:|---|
| 1 | 脱敏 Key |
| 2 | 产品 |
| 3 | 授权类型 |
| 4 | 状态 |
| 5 | 开始时间 |
| 6 | 到期时间 |
| 7 | 最大设备 |
| 8 | 最大并发 |
| 9 | 创建时间 |
| 10 | 操作 |

- 操作列固定在右侧。
- 行操作第一期只提供“查看详情”。
- `display_key` 必须使用等宽字体。
- 到期时间为空时显示“永久”。
- 当前接口没有 `total`，底部只显示上一页、下一页和当前偏移范围，不显示伪造总页数。

### PAGE-007 Key 生成与一次性交付

生成表单：

- 产品。
- 授权策略。
- 数量（批量时 1～500）。
- 最大设备数覆盖。
- 最大并发数覆盖。
- 功能授权。
- metadata 高级设置。

生成成功后进入不可跳过的一次性交付视图：

```text
完整 Key 仅在本次响应中显示一次
[复制全部] [下载 TXT/CSV] [我已安全保存]
```

安全规则：

1. 页面刷新后不尝试重新获取明文。
2. 明文不写入 LocalStorage、IndexedDB、URL 或浏览器日志。
3. 离开前提示用户确认已安全保存。
4. 单个 Key 提供复制按钮。
5. 批量 Key 提供复制全部和本地文件下载。
6. 下载文件由浏览器根据本次响应生成，不要求后端再次返回明文。

### PAGE-008 Key 详情与设备

使用 UI-03 详情抽屉思想；从列表点击后可打开详情抽屉，复杂设备管理可以进入完整详情页。

信息区：

- 脱敏 Key。
- 状态和授权类型。
- 产品与策略 ID。
- 开始、到期、激活、吊销时间。
- 最大设备、最大并发、离线宽限。
- 自助解绑规则。
- 功能授权和限制。
- metadata。

操作区根据状态显示：

- 暂停。
- 恢复。
- 续期。
- 永久吊销。

不能提供：

- 查看完整 Key。
- 修改 Key 字符串。
- 恢复已永久吊销 Key。
- 删除 Key。

设备区：

- 设备列表。
- 激活状态过滤。
- 强制解绑、封禁、解封。
- 所有设备危险操作必须输入原因。
- 弹窗必须说明会话撤销影响。

### PAGE-009 管理员审计日志

- 高密度表格。
- 顶部高级筛选折叠区。
- 点击行打开详情抽屉。
- JSON 前后数据使用格式化代码块或差异视图。
- 显示并允许复制 `request_id`。
- 不提供修改、删除和导出按钮，直到接口支持。

### PAGE-010 授权事件

- 表格 + 高级筛选。
- SUCCESS/FAILURE 状态标签。
- 关联 UUID 可复制，并在对应页面存在时提供内部跳转。
- 详情抽屉显示 metadata。
- 不提供修改和删除。

### PAGE-011 接口中心

- 只读接口目录。
- 分为系统状态、管理员 API、客户端授权协议三组。
- 显示方法、路径、用途、认证方式和安全提醒。
- 可复制接口路径。
- 第一版不提供绕过设备签名的在线调试器。

### PAGE-012 通用状态页

- 401：跳转登录页。
- 403：显示无权限并提供返回按钮。
- 404：显示页面不存在。
- 503：显示基础设施未就绪，可重试。
- 未知错误：显示错误信息与 `request_id`。

---

## 7. 表单与交互规则

### 7.1 表单

- 标签固定显示在输入框上方。
- 必填字段显示 `*`。
- 校验错误显示在字段下方。
- 服务端错误不得只用 Toast，字段错误应回填到对应字段。
- 保存期间按钮进入加载状态并防止重复提交。
- 成功后刷新对应数据，不做整页刷新。

### 7.2 危险操作等级

| 等级 | 操作 | 交互 |
|---|---|---|
| 普通 | 新建、编辑、刷新 | 直接表单提交 |
| 警告 | 暂停、恢复、续期 | 普通确认弹窗 |
| 高危 | 强制解绑、封禁、解封 | 输入原因 + 影响说明 + 确认 |
| 不可恢复 | 永久吊销 Key | 红色危险弹窗 + 输入原因 + 明确不可恢复 |

### 7.3 时间显示

- 默认按浏览器本地时区显示。
- 格式：`YYYY-MM-DD HH:mm:ss`。
- 悬停可显示原始 ISO-8601 时间。
- 空值统一显示 `—`；永久授权的到期时间显示“永久”。

### 7.4 UUID 与 Key

- UUID 和脱敏 Key 使用等宽字体。
- 默认截断，悬停显示完整值。
- 提供复制按钮。
- 永远不提供再次查看完整 Key 的入口。

---

## 8. 页面状态基线

每个数据页面必须实现以下状态：

| 状态 | 设计要求 |
|---|---|
| 首次加载 | 使用骨架屏，避免整页旋转图标 |
| 局部刷新 | 保留现有数据并显示局部加载状态 |
| 空数据 | 说明原因；有创建权限时给真实创建按钮 |
| 筛选无结果 | 保留筛选条件，提供清除筛选 |
| 参数错误 | 显示具体字段错误 |
| 未登录 | 清理前端用户状态并跳转登录 |
| 无权限 | 显示 403，不反复重试 |
| 资源不存在 | 返回列表并提示资源可能已删除或不属于当前租户 |
| 服务未就绪 | 显示 PostgreSQL/Redis 组件状态并允许重试 |
| 网络错误 | 保留用户输入并允许重试 |
| 未知错误 | 显示 `request_id` 便于查日志 |

---

## 9. 响应式基线

### ≥ 1280px

- 完整左侧导航 240px。
- 多列卡片。
- Key 表格完整列。
- 详情使用右侧抽屉。

### 768～1279px

- 左侧导航折叠为 72px 或抽屉导航。
- 卡片两列。
- 表格允许横向滚动。
- 次要列可隐藏，但不能隐藏状态和主要操作。

### < 768px

- 顶部移动菜单。
- 页面标题和操作按钮分两行。
- 筛选区垂直排列。
- 数据表可切换为卡片列表。
- 抽屉改为全屏面板。
- 危险确认弹窗仍必须完整显示影响和原因字段。

管理后台以桌面使用为主，但不得在手机端出现无法关闭弹窗或无法操作表单的问题。

---

## 10. 可访问性基线

1. 正文与背景对比度至少满足 WCAG AA。
2. 所有按钮和输入框必须可用键盘操作。
3. 焦点状态必须可见。
4. 图标按钮必须有文本提示或 `aria-label`。
5. 状态不能只靠颜色区分。
6. 表单错误需要与字段建立语义关联。
7. 弹窗打开后焦点进入弹窗，关闭后返回触发按钮。
8. 危险操作按钮不能与普通主按钮颜色相同。

---

## 11. 接口与页面绑定

| 页面 | 接口 |
|---|---|
| PAGE-001 登录 | 依赖 DEP-001～DEP-004 新接口 |
| PAGE-002 后台首页 | `/health`、`/ready`、`/admin/v1/license-events`；统计依赖 DEP-007 |
| PAGE-003 产品列表 | `POST/GET /admin/v1/products` |
| PAGE-004 产品详情 | 产品详情、修改、版本和功能共 8 个相关接口 |
| PAGE-005 授权策略 | 3 个 `/admin/v1/license-policies` 接口 |
| PAGE-006 Key 管理 | `GET /admin/v1/license-keys` |
| PAGE-007 Key 生成与交付 | 单个生成和批量生成接口 |
| PAGE-008 Key 详情与设备 | Key 详情/生命周期 5 个接口 + 设备 4 个接口 |
| PAGE-009 审计日志 | `GET /admin/v1/audit-logs` |
| PAGE-010 授权事件 | `GET /admin/v1/license-events` |
| PAGE-011 接口中心 | 已识别全部 36 个接口的只读说明 |

完整接口字段映射以 `10-UI功能与接口识别报告.md` 为准。

---

## 12. 不允许擅自改变的冻结规则

用户确认本基线后，以下规则被冻结：

1. 全局视觉以后台 UI-01 为主。
2. Key 管理内容区采用后台 UI-03 高密度工作台。
3. 全局统一使用 UI-01 左侧导航，不在 Key 页面切换另一套导航。
4. 主色为科技蓝，浅色模式为默认主题。
5. 导航顺序和信息架构不随意调整。
6. 不设计后端不支持的业务按钮。
7. Key 明文只在生成后一次性交付。
8. 永久吊销必须使用不可恢复的危险确认流程。
9. 强制解绑、封禁和解封必须填写原因。
10. 当前没有 `total` 时不得显示伪造总页数。
11. 当前没有统计接口时不得显示伪造业务统计。
12. 浏览器不得接触 `MANAGEMENT_GATEWAY_TOKEN`。
13. 页面必须具备加载、空数据、错误、未登录和无权限状态。
14. 正式实现必须保持现有 36 个接口和授权业务逻辑兼容。

修改冻结规则必须建立 `CR-编号` 变更单。

---

## 13. 后续可优化内容

以下内容不属于第一版强制范围，可在后端接口支持后新增：

- 深色模式。
- 仪表盘完整统计图表。
- 租户切换器和租户管理。
- 管理员、角色和权限管理。
- 全局设备中心。
- 在线会话中心。
- Key 关键字和批次搜索。
- 完整页码分页。
- 审计日志和授权事件导出。
- API 在线调试器。
- 多语言。

---

## 14. 开发模块顺序建议

UI 基线确认后，交由 1号人格依次建立模块白皮书：

```text
MOD-UI-001 管理员认证与后台壳
    ↓
MOD-UI-002 仪表盘与服务状态
    ↓
MOD-UI-003 产品、版本与功能管理
    ↓
MOD-UI-004 授权策略管理
    ↓
MOD-UI-005 Key 生成、列表与一次性交付
    ↓
MOD-UI-006 Key 详情与生命周期
    ↓
MOD-UI-007 设备管理
    ↓
MOD-UI-008 审计与授权事件
    ↓
MOD-UI-009 接口中心
    ↓
MOD-UI-010 Linux 部署整合
```

第一开发模块应优先解决登录安全闭环和后台公共外壳，否则后续页面无法安全连接现有管理员 API。

---

## 15. 基线确认门禁

当前状态：**已确认并冻结**。

确认依据：用户在选定“以后台 UI-01 为主，采用后台 UI-03 的 Key 管理页面”后发送“开始制作”。该命令按 V5.0 普通阶段的“执行/进入下一阶段”处理。

当前人格：**1号人格（产品经理 / 系统架构师 / 项目总控）**。

本基线已经冻结。后续若要修改第 12 章列出的冻结规则，必须先建立 `CR-编号` 变更申请。

已切换至 1号人格，进入技术架构、接口数据库、开发计划与需求冻结准备阶段。即使文档准备完成，只有收到：

```text
批准开发 MOD-UI-001
```

才能开始编写正式代码。
