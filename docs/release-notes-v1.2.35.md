# APPGOG打包授权系统 v1.2.35

日期：2026-09-26

- 修复客户主题每次刷新显示授权检查文案；正常验证使用安静的页面加载状态，失败后仍显示明确激活/错误界面。
- 签名包和域名匹配后，将只读健康、运行状态与加密包身份检查并行，在 DOM 就绪前开始。
- 带哈希验证的包身份资源使用正常浏览器缓存；每次仍验证其 SHA-256 和 AES-GCM 内容。
- 已激活管理员页面不再重复查询插件、注册包；已保存管理路径继续保留。
- 修复服务端激活状态丢失后仍合并浏览器旧凭证的边界。保留签名、域名、安装身份及两阶段激活检查。

## 更新说明

运行环境合同、数据库、签名密钥和授权桥1.0.8不变。程序升级后需重新构建并安装客户主题，既有下载ZIP不会被中心自动改写。部署保留用户、设置、Key和构建记录。

## 验证

覆盖DOM就绪前并行、管理员不重复注册、服务端拒绝/缺失、异机身份、篡改凭证、旧桥安装升级、两阶段激活、真实主题包与全量发布测试。
