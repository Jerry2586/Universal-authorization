# APPGOG打包授权系统 v1.2.37

日期：2026-09-26

- 按参考图重塑成品“授权与更新”页面：深色蓝黑底、紫蓝渐变状态横卡、等宽身份/版本双卡及底部安全分区。支持浅色与手机单列。
- APPGOG用户面板设置下增加独立授权路由，沿用原登录保护；主题编辑器入口复用卡片视图。
- 复制安装身份、签名更新检查、打包中心链接和迁移说明具有明确反馈。
- 检查更新增加current/available/stale/unavailable/restricted状态与并发合并，失败不得误报最新。
- 到期字段明确以授权中心为准，短期凭证时间单独说明；迁移为指引，不执行新服务器操作。

## 升级

环境和数据库不变，桥仍1.0.8。平台更新后重新构建主题即可包含新页面，旧下载ZIP不自动改写。原授权方式、原业务路由、用户设置和无闪白启动保持。

## 验证

覆盖签名更新状态、路由接入和失败边界、本地桌面/手机/浅色、按钮及路由往返、真实主题构建、全量测试和Docker更新保留。
