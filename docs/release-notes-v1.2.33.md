# APPGOG打包授权系统 v1.2.33

日期：2026-09-26

- 修复已使用安装 Key 后“领取交付”只弹提示：构建结果和包级激活状态分开显示；未使用成品提供下载与激活，已解锁、已激活、被替代和撤销成品提供实际详情。
- 运营与打包中心工单加入接收方未读数量、列表红标和显式逐工单已读确认，持久化游标保留跨登录状态，内部备注不通知客户，并发新回复不会被旧确认吞掉。
- 授权运行时兼容旧版主题后台连接信息、同源父窗口及授权桥认证上下文；插件安装失败提供地址补全、后台登录、重试和进度反馈。
- 安装 Key 解锁后自动进入主题编辑器，保留后台连接；已激活主题编辑器设置菜单下提供“授权与激活”。
- 授权桥 1.0.5 使用服务端中间件扩展原生 Xboard 管理页面：主题卡片授权入口打开居中窗口，原生激活成功后继续激活流程。识别不带新版元数据的历史受保护主题，升级迁移触发常驻进程重载。
- 固定 Key、Install Key、Install Receipt、域名和服务器身份协议不变；程序升级保留业务数据库、主题配置、密钥和安装身份。Support 新表和消息序号为向前兼容迁移。

边界：原生 Xboard 在首次安装授权桥之前没有 APPGOG 管理扩展，因此首次需从已登录后台进入主题页完成自动插件准备；后台路径无法自动取得时提供一次连接补全，不能声称在无插件、无登录信息下自动修改第三方后台。新运行时与设置入口需要重新构建主题成品；仅更新中心不会重写已下载或已安装主题。激活后撤销宽限期自动删除和全产品业务路由 Guard 不属于本次完成范围。
