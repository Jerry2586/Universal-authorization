# APPGOG打包授权系统 v1.2.18

日期：2026-09-25

本地修复版本，尚未创建正式发布附件或 GitHub Release。

- 修复独立打包中心漏发客户入口及递归 ES Module，登录事件可以正常加载；静态资源保持客户模块白名单。
- 两个 HTTP 入口共用客户端 IP 和请求编号规则，代理保留错误合同、Cookie、下载校验头和请求编号；增加上游超时与连接中断处理。
- 打包中心 /health 检查静态资源、授权中心可用性和节点凭证，异常返回 503。
- npm start 默认启动授权中心、打包中心和 Worker；start:combined 保留单体兼容。专用启动命令固定对应角色，拆分启动先拒绝端口占用或冲突。
- 默认公开地址跟随角色和实际端口，保留人工配置的公开域名。
- 新增静态模块递归加载、代理上下文、错误、就绪、超时、端口和角色回归测试；真实拆分构建测试增加节点就绪断言。

此次修复不变更数据库结构、授权 Key、管理员账号、签名身份或运行环境发布合同。正式发布仍须完成全量验证、制品签名、Ubuntu/Docker CI 和 Release 回下载验证。
