# APPGOG打包授权系统 v1.2.29

日期：2026-09-26

- 修复构建 Worker 在耗时主题混淆后可能复用失效空闲连接的问题。任务领取、进度、文件传输和完成回报都使用独立 HTTP 连接，避免 UND_ERR_SOCKET 导致已构建任务失败。
- 不自动重放领取与完成等状态请求，保留超时和脱敏诊断。
- 增加真实 HTTP 构建连接隔离及领取响应丢失不重复执行的回归验证。
- 沿用 v1.2.28 的套餐、界面、构建生命周期和授权桥功能；固定 Key、安装凭据、两阶段激活和授权边界不变。
- 升级保留数据库、用户、授权、设置、上传、构建、密钥与备份，环境依赖继续遵循 release-contract.json。

验收限制：安装窗口自动清理仍依赖 Xboard 启用授权桥和调度器；离线撤销即时性及激活后撤销宽限期自动删除的限制与 v1.2.28 相同。
