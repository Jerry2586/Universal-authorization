# APPGOG打包授权系统 v1.2.31

日期：2026-09-26

- 修复真实 Xboard 安装验收发现的刷新回归：已激活页面不再读取管理员完整授权状态，只消费签名运行状态；浏览器刷新后继续清理 Refresh Secret、Install Receipt Secret 和安装窗口 Token。服务端桥保留刷新所需凭据。
- 增加安装解锁、固定 Key 激活、刷新第三次启动的完整回归，验证后台路径、安装身份、解锁状态和服务端刷新凭据同时保留。
- 沿用 v1.2.30 的三个入口预锁定、桥升级重载和错误分类修复。固定 Key、一次性安装 Key、域名和 Installation ID 语义不变。
- 环境合同不变；升级保留数据库、用户、授权、密钥、配置、上传与构建。

验收边界：未激活清理仍依赖授权桥与 Xboard 调度器；离线撤销即时性、激活后撤销宽限期自动删除、原生主题按钮定制和产品全部服务端路由 Guard 不属于本次完成范围。
