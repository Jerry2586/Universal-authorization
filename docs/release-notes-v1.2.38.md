# APPGOG打包授权系统 v1.2.38

日期：2026-09-26

- 修复真实部署后 CDN 仍返回旧面板脚本，导致新授权菜单不出现的问题。
- APPGOG 每次构建自动刷新本地 JS/CSS 引用版本，同时更新 Blade、HTML 与动态加载器；重复构建同一产品版本也使用新的构建标识。
- 外部 CDN 地址不修改；不兼容的动态加载器明确阻断构建，禁止部分更新。
- 包含 v1.2.37 的参考图授权与更新页面；授权方式、数据及主题设置不变。

## 验证

覆盖旧版 11910 静态与动态缓存键同步、同版本重建、第三方资源保持；全量测试、真实主题包及 Docker 流程。
