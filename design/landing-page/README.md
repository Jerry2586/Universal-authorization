# 独立介绍页存档

保留原介绍页的布局、文案、样式和品牌素材，供后续单独使用。双击 index.html 即可预览，无需启动后台。

此目录不属于两个中心的公开静态目录。页面不调用业务 API，不连接登录会话；原来的两个中心入口保留外观但取消跳转。样式与图片为独立快照，不随两个中心的后续 UI 修改变化。

运营入口为 /admin，打包入口为 /build。本地单体根地址直接显示运营页面；生产拆分部署继续沿用各中心自己的入口。
